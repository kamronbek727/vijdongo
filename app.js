
// --- Translations ---
let loadedTranslations = {
    uz: {
    "common": {
        "continueBtn": "Davom etish",
        "backBtn": "Ortga",
        "confirmBtn": "Tasdiqlash",
        "cancelBtn": "Bekor qilish",
        "yes": "Ha",
        "no": "Yo'q",
        "saveBtn": "Saqlash",
        "finishBtn": "Tugatish",
        "selectedAddress": "Tanlangan manzil",
        "addressLoading": "Manzil aniqlanmoqda...",
        "addressNotFound": "Manzil topilmadi",
        "selectAddress": "Manzilni tanlang",
        "gpsLocating": "Joylashuv aniqlanmoqda...",
        "gpsNotSupported": "Brauzeringiz joylashuvni aniqlashni qo'llab-quvvatlamaydi.",
        "mapNotLoaded": "Xarita hali yuklanmadi. Iltimos, kutib turing.",
        "errorGeneric": "Xatolik yuz berdi",
        "currency": "so'm",
        "km": "km",
        "m": "m"
    },
    "gps": {
        "errorGeneric": "Joylashuvni aniqlashda xatolik yuz berdi.",
        "errorPermissionDenied": "Joylashuvga ruxsat berilmadi. Iltimos, brauzer sozlamalaridan ruxsat bering.",
        "errorPositionUnavailable": "Joylashuvni aniqlash imkonsiz. GPS yoqilganligini tekshiring.",
        "errorTimeout": "Joylashuvni aniqlash vaqti tugadi. Qayta urinib ko'ring."
    },
    "nav": {
        "home": "Asosiy",
        "trips": "Safarlarim",
        "messages": "Xabarlar",
        "profile": "Profil"
    },
    "regions": {
        "toshkent_sh": "Toshkent shahri",
        "toshkent_v": "Toshkent viloyati",
        "andijon": "Andijon",
        "fargona": "Farg‘ona",
        "namangan": "Namangan",
        "samarqand": "Samarqand",
        "buxoro": "Buxoro",
        "navoiy": "Navoiy",
        "qashqadaryo": "Qashqadaryo",
        "surxondaryo": "Surxondaryo",
        "jizzax": "Jizzax",
        "sirdaryo": "Sirdaryo",
        "xorazm": "Xorazm",
        "qoraqalpogiston": "Qoraqalpog‘iston Respublikasi"
    },
    "splash": {
        "startJourney": "Safarni boshlash"
    },
    "login": {
        "title": "Tizimga kirish",
        "phoneLabel": "Telefon raqami",
        "phoneDescription": "Safar buyurtma qilish va hisobingizga kirish uchun telefon raqamingizni kiriting",
        "phoneError": "Telefon raqamini to'liq kiriting.",
        "nameSurnameTitle": "Ism va familiya",
        "nameSurnameDescription": "Iltimos, ismingiz va familiyangizni kiriting",
        "nameLabel": "Ism",
        "namePlaceholder": "Ismingizni kiriting",
        "nameError": "Ismingizni kiriting (kamida 2 ta harf).",
        "surnameLabel": "Familiya",
        "surnamePlaceholder": "Familiyangizni kiriting",
        "genderLabel": "Jinsingiz",
        "genderDescription": "Profil ma'lumotlarini to'ldirish uchun jinsingizni tanlang",
        "genderError": "Iltimos, jinsingizni tanlang.",
        "male": "Erkak",
        "maleDesc": "Erkaklar uchun profil rasmi",
        "female": "Ayol",
        "femaleDesc": "Ayollar uchun profil rasmi",
        "langSelectTitle": "Tilni tanlang",
        "langSelectDescription": "Ilova interfeysi uchun qulay tilni tanlang",
        "uzbek": "O‘zbekcha 🇺🇿",
        "russian": "Русский 🇷🇺",
        "english": "English 🇺🇸"
    },
    "home": {
        "title": "Vijdon GO",
        "greeting": "Assalomu alaykum!",
        "greetingSub": "Bugun qayerga boramiz?",
        "greetingUser": "Assalomu alaykum, {name}!",
        "services": {
            "taxi": "Taxi",
            "delivery": "Dostavka, Pochta"
        },
        "promoBanner": "Promo Banner"
    },
    "taxi": {
        "title": "Taxi",
        "fromLabel": "Qayerdan",
        "fromPlaceholder": "Qayerdasiz?",
        "toLabel": "Qayerga",
        "toPlaceholder": "Qayerga borasiz?",
        "emptyTitle": "Manzillarni tanlang",
        "emptyDesc": "Olib ketish va borish joyini ko'rsating. Shundan so'ng narx va marshrut paydo bo'ladi.",
        "calcRoute": "Yo'nalish hisoblanmoqda...",
        "priceLabel": "Narxi:",
        "passengers": "Yo'lovchilar soni",
        "baggage": "Bagaj kerakmi?",
        "orderBtn": "Buyurtma berish",
        "loadingRoute": "Yo'l hisoblanmoqda...",
        "selectPickupFirst": "Avval qayerdan ketishingizni (Qayerdasiz?) tanlang!",
        "waitRouteCalc": "Yo'l hali hisoblanmoqda, iltimos kuting...",
        "addressWaitOrMap": "Iltimos, manzil aniqlanishini kuting yoki xaritadan tanlang.",
        "selectAddressesFirst": "Iltimos, avval jo'nash va borish manzillarini to'liq tanlang!",
        "invalidCoords": "Manzil koordinatalari xato! Iltimos, manzillarni xaritadan qayta tanlang.",
        "tariffs": {
            "start": "START",
            "startDesc": "Oddiy avto",
            "komfort": "KOMFORT",
            "komfortDesc": "Qulay avto",
            "biznes": "BIZNES",
            "biznesDesc": "Premium"
        }
    },
    "delivery": {
        "title": "Dostavka",
        "bannerTitle": "Pochta va Posylka",
        "bannerSub": "Tez va ishonchli yetkazib berish xizmati",
        "fromPlaceholder": "Pochta qayerdan?",
        "toPlaceholder": "Pochta qayerga?",
        "tariffName": "DOSTAVKA",
        "tariffSub": "Pochta yetkazish",
        "tariffRate": "100 so'm/km",
        "orderBtn": "Dostavka chaqirish",
        "loadingRoute": "Yo'l hisoblanmoqda...",
        "selectAddressesFirst": "Iltimos, pochta olib ketish va yetkazish manzillarini tanlang!"
    },
    "pickup": {
        "title": "Qayerdasiz?"
    },
    "dest": {
        "title": "Qayerga borasiz?"
    },
    "waiting": {
        "searchDriver": "Haydovchi qidirilmoqda...",
        "searchDesc": "Iltimos, kuting. Sizning yo'nalishingiz bo'yicha haydovchilarga so'rov yuborildi."
    },
    "trip": {
        "statusActive": "Safar davom etmoqda",
        "statusFinished": "YETIB KELDINGIZ",
        "statusDriverEnRoute": "HAYDOVCHI YO'LDA",
        "statusError": "XATOLIK: YO'L TOPILMADI",
        "driverName": "Haydovchi",
        "carInfo": "Avtomobil",
        "cancelBtn": "Safarni bekor qilish",
        "kmLeft": "km qoldi",
        "mLeft": "m qoldi"
    },
    "trips": {
        "title": "Safarlarim",
        "noTrips": "Sizda hali safarlar yo'q.",
        "completed": "Yakunlangan",
        "active": "Faol safar"
    },
    "messages": {
        "title": "Xabarlar",
        "supportTitle": "Qo'llab-quvvatlash xizmati",
        "supportWelcome": "Assalomu alaykum, sizga qanday yordam bera olaman?",
        "promoTitle": "Aksiya va chegirmalar",
        "promoWelcome": "Yangi yil chegirmalari boshlandi!",
        "driverOnWay": "Yo'ldaman, 5 daqiqada boraman."
    },
    "profile": {
        "title": "Profil",
        "defaultName": "Foydalanuvchi",
        "settings": "Sozlamalar",
        "help": "Yordam",
        "logout": "Tizimdan chiqish"
    },
    "settings": {
        "title": "Sozlamalar",
        "language": "Til / Language",
        "phoneUnchangeable": "Telefon raqami (O'zgartirib bo'lmaydi)",
        "savedSuccess": "O'zgarishlar saqlandi!"
    },
    "call": {
        "status": "Chaqirilmoqda..."
    },
    "chat": {
        "inputPlaceholder": "Xabar yozing...",
        "online": "Online",
        "edit": "Tahrirlash",
        "delete": "O'chirish",
        "edited": "(tahrirlandi)",
        "driverWelcome": "Assalomu alaykum! Men keldim, qayerdasiz?",
        "confirmDelete": "Xabarni o'chirmoqchimisiz?",
        "editPrompt": "Xabarni tahrirlash:",
        "supportReply1": "Muammoingizni yozib qoldiring, tez orada javob beramiz.",
        "supportReply2": "Operatorlarimiz hozir band, iltimos kuting.",
        "supportReply3": "Xabaringiz qabul qilindi. Operator tez orada siz bilan bog'lanadi.",
        "driverReply1": "Xo'p tushunarli.",
        "driverReply2": "Hozir yetib boraman.",
        "driverReply3": "Yo'ldaman.",
        "driverReply4": "Manzilni aniqroq ayta olasizmi?"
    }
},
    ru: {
    "common": {
        "continueBtn": "Продолжить",
        "backBtn": "Назад",
        "confirmBtn": "Подтвердить",
        "cancelBtn": "Отменить",
        "yes": "Да",
        "no": "Нет",
        "saveBtn": "Сохранить",
        "finishBtn": "Завершить",
        "selectedAddress": "Выбранный адрес",
        "addressLoading": "Определение адреса...",
        "addressNotFound": "Адрес не найден",
        "selectAddress": "Выберите адрес",
        "gpsLocating": "Определение местоположения...",
        "gpsNotSupported": "Ваш браузер не поддерживает геолокацию.",
        "mapNotLoaded": "Карта еще не загрузилась. Пожалуйста, подождите.",
        "errorGeneric": "Произошла ошибка",
        "currency": "сум",
        "km": "км",
        "m": "м"
    },
    "gps": {
        "errorGeneric": "Произошла ошибка при определении местоположения.",
        "errorPermissionDenied": "Доступ к местоположению запрещен. Пожалуйста, разрешите в настройках браузера.",
        "errorPositionUnavailable": "Невозможно определить местоположение. Проверьте, включен ли GPS.",
        "errorTimeout": "Истекло время ожидания определения местоположения. Попробуйте снова."
    },
    "nav": {
        "home": "Главная",
        "trips": "Поездки",
        "messages": "Сообщения",
        "profile": "Профиль"
    },
    "regions": {
        "toshkent_sh": "Город Ташкент",
        "toshkent_v": "Ташкентская область",
        "andijon": "Андижан",
        "fargona": "Фергана",
        "namangan": "Наманган",
        "samarqand": "Самарканд",
        "buxoro": "Бухара",
        "navoiy": "Навои",
        "qashqadaryo": "Кашкадарья",
        "surxondaryo": "Сурхандарья",
        "jizzax": "Джизак",
        "sirdaryo": "Сырдарья",
        "xorazm": "Хорезм",
        "qoraqalpogiston": "Республика Каракалпакстан"
    },
    "splash": {
        "startJourney": "Начать поездку"
    },
    "login": {
        "title": "Вход в систему",
        "phoneLabel": "Номер телефона",
        "phoneDescription": "Введите ваш номер телефона для заказа поездок и входа в систему",
        "phoneError": "Введите номер телефона полностью.",
        "nameSurnameTitle": "Имя и фамилия",
        "nameSurnameDescription": "Пожалуйста, введите ваше имя и фамилию",
        "nameLabel": "Имя",
        "namePlaceholder": "Введите ваше имя",
        "nameError": "Введите ваше имя (минимум 2 буквы).",
        "surnameLabel": "Фамилия",
        "surnamePlaceholder": "Введите вашу фамилию",
        "genderLabel": "Ваш пол",
        "genderDescription": "Выберите ваш пол для заполнения профиля",
        "genderError": "Пожалуйста, выберите ваш пол.",
        "male": "Мужской",
        "maleDesc": "Фото профиля для мужчин",
        "female": "Женский",
        "femaleDesc": "Фото профиля для женщин",
        "langSelectTitle": "Выберите язык",
        "langSelectDescription": "Выберите подходящий язык для интерфейса приложения",
        "uzbek": "O‘zbekcha 🇺🇿",
        "russian": "Русский 🇷🇺",
        "english": "English 🇺🇸"
    },
    "home": {
        "title": "Vijdon GO",
        "greeting": "Здравствуйте!",
        "greetingSub": "Куда поедем сегодня?",
        "greetingUser": "Здравствуйте, {name}!",
        "services": {
            "taxi": "Такси",
            "delivery": "Доставка, Почта"
        },
        "promoBanner": "Промо Баннер"
    },
    "taxi": {
        "title": "Такси",
        "fromLabel": "Откуда",
        "fromPlaceholder": "Где вы?",
        "toLabel": "Куда",
        "toPlaceholder": "Куда вы едете?",
        "emptyTitle": "Выберите адреса",
        "emptyDesc": "Укажите места посадки и высадки. После этого появятся цена и маршрут.",
        "calcRoute": "Рассчитывается маршрут...",
        "priceLabel": "Цена:",
        "passengers": "Количество пассажиров",
        "baggage": "Нужен багаж?",
        "orderBtn": "Заказать такси",
        "loadingRoute": "Рассчитывается маршрут...",
        "selectPickupFirst": "Сначала выберите, откуда вы отправляетесь (Где вы?)!",
        "waitRouteCalc": "Маршрут еще рассчитывается, пожалуйста, подождите...",
        "addressWaitOrMap": "Пожалуйста, подождите определения адреса или выберите его на карте.",
        "selectAddressesFirst": "Пожалуйста, сначала полностью выберите адреса отправления и назначения!",
        "invalidCoords": "Неверные координаты адреса! Пожалуйста, выберите адреса на карте заново.",
        "tariffs": {
            "start": "СТАРТ",
            "startDesc": "Обычное авто",
            "komfort": "КОМФОРТ",
            "komfortDesc": "Комфортное авто",
            "biznes": "БИЗНЕС",
            "biznesDesc": "Премиум"
        }
    },
    "delivery": {
        "title": "Доставка",
        "bannerTitle": "Почта и Посылка",
        "bannerSub": "Быстрая и надежная служба доставки",
        "fromPlaceholder": "Откуда посылка?",
        "toPlaceholder": "Куда посылка?",
        "tariffName": "ДОСТАВКА",
        "tariffSub": "Доставка почты",
        "tariffRate": "100 сум/км",
        "orderBtn": "Вызвать доставку",
        "loadingRoute": "Рассчитывается маршрут...",
        "selectAddressesFirst": "Пожалуйста, выберите адреса забора и доставки посылки!"
    },
    "pickup": {
        "title": "Где вы находитесь?"
    },
    "dest": {
        "title": "Куда вы направляетесь?"
    },
    "waiting": {
        "searchDriver": "Поиск водителя...",
        "searchDesc": "Пожалуйста, подождите. Ваш запрос отправлен водителям."
    },
    "trip": {
        "statusActive": "Поездка продолжается",
        "statusFinished": "ВЫ ПРИБЫЛИ",
        "statusDriverEnRoute": "ВОДИТЕЛЬ В ПУТИ",
        "statusError": "ОШИБКА: МАРШРУТ НЕ НАЙДЕН",
        "driverName": "Водитель",
        "carInfo": "Автомобиль",
        "cancelBtn": "Отменить поездку",
        "kmLeft": "км осталось",
        "mLeft": "м осталось"
    },
    "trips": {
        "title": "Мои поездки",
        "noTrips": "У вас еще нет поездок.",
        "completed": "Завершено",
        "active": "Активная поездка"
    },
    "messages": {
        "title": "Сообщения",
        "supportTitle": "Служба поддержки",
        "supportWelcome": "Здравствуйте, чем я могу вам помочь?",
        "promoTitle": "Акции и скидки",
        "promoWelcome": "Новогодние скидки начались!",
        "driverOnWay": "В пути, буду через 5 минут."
    },
    "profile": {
        "title": "Профиль",
        "defaultName": "Пользователь",
        "settings": "Настройки",
        "help": "Помощь",
        "logout": "Выйти из системы"
    },
    "settings": {
        "title": "Настройки",
        "language": "Язык / Language",
        "phoneUnchangeable": "Номер телефона (Нельзя изменить)",
        "savedSuccess": "Изменения сохранены!"
    },
    "call": {
        "status": "Вызов..."
    },
    "chat": {
        "inputPlaceholder": "Напишите сообщение...",
        "online": "В сети",
        "edit": "Редактировать",
        "delete": "Удалить",
        "edited": "(изменено)",
        "driverWelcome": "Здравствуйте! Я приехал, где вы?",
        "confirmDelete": "Вы хотите удалить сообщение?",
        "editPrompt": "Редактировать сообщение:",
        "supportReply1": "Оставьте вашу проблему, мы ответим в ближайшее время.",
        "supportReply2": "Наши операторы сейчас заняты, пожалуйста, подождите.",
        "supportReply3": "Ваше сообщение принято. Оператор свяжется с вами в ближайшее время.",
        "driverReply1": "Хорошо, понял.",
        "driverReply2": "Сейчас буду.",
        "driverReply3": "Я в пути.",
        "driverReply4": "Можете уточнить адрес?"
    }
},
    en: {
    "common": {
        "continueBtn": "Continue",
        "backBtn": "Back",
        "confirmBtn": "Confirm",
        "cancelBtn": "Cancel",
        "yes": "Yes",
        "no": "No",
        "saveBtn": "Save",
        "finishBtn": "Finish",
        "selectedAddress": "Selected address",
        "addressLoading": "Locating address...",
        "addressNotFound": "Address not found",
        "selectAddress": "Select address",
        "gpsLocating": "Determining location...",
        "gpsNotSupported": "Your browser does not support geolocation.",
        "mapNotLoaded": "The map has not loaded yet. Please wait.",
        "errorGeneric": "An error occurred",
        "currency": "sum",
        "km": "km",
        "m": "m"
    },
    "gps": {
        "errorGeneric": "An error occurred while determining location.",
        "errorPermissionDenied": "Location permission denied. Please enable it in browser settings.",
        "errorPositionUnavailable": "Unable to determine location. Check if GPS is enabled.",
        "errorTimeout": "Location request timed out. Please try again."
    },
    "nav": {
        "home": "Home",
        "trips": "My Trips",
        "messages": "Messages",
        "profile": "Profile"
    },
    "regions": {
        "toshkent_sh": "Tashkent City",
        "toshkent_v": "Tashkent Region",
        "andijon": "Andijan",
        "fargona": "Fergana",
        "namangan": "Namangan",
        "samarqand": "Samarkand",
        "buxoro": "Bukhara",
        "navoiy": "Navoi",
        "qashqadaryo": "Kashkadarya",
        "surxondaryo": "Surkhandarya",
        "jizzax": "Jizzakh",
        "sirdaryo": "Sirdarya",
        "xorazm": "Khorezm",
        "qoraqalpogiston": "Republic of Karakalpakstan"
    },
    "splash": {
        "startJourney": "Start Journey"
    },
    "login": {
        "title": "Login",
        "phoneLabel": "Phone number",
        "phoneDescription": "Enter your phone number to order rides and log in",
        "phoneError": "Enter your phone number completely.",
        "nameSurnameTitle": "Name and Surname",
        "nameSurnameDescription": "Please enter your first name and last name",
        "nameLabel": "Name",
        "namePlaceholder": "Enter your name",
        "nameError": "Enter your name (at least 2 characters).",
        "surnameLabel": "Surname",
        "surnamePlaceholder": "Enter your surname",
        "genderLabel": "Gender",
        "genderDescription": "Select your gender to complete your profile",
        "genderError": "Please select your gender.",
        "male": "Male",
        "maleDesc": "Avatar image for males",
        "female": "Female",
        "femaleDesc": "Avatar image for females",
        "langSelectTitle": "Choose Language",
        "langSelectDescription": "Choose a convenient language for the app interface",
        "uzbek": "O‘zbekcha 🇺🇿",
        "russian": "Русский 🇷🇺",
        "english": "English 🇺🇸"
    },
    "home": {
        "title": "Vijdon GO",
        "greeting": "Hello!",
        "greetingSub": "Where are we going today?",
        "greetingUser": "Hello, {name}!",
        "services": {
            "taxi": "Taxi",
            "delivery": "Delivery, Mail"
        },
        "promoBanner": "Promo Banner"
    },
    "taxi": {
        "title": "Taxi",
        "fromLabel": "From",
        "fromPlaceholder": "Where are you?",
        "toLabel": "To",
        "toPlaceholder": "Where are you going?",
        "emptyTitle": "Select locations",
        "emptyDesc": "Indicate pickup and drop-off locations. Price and route will appear after.",
        "calcRoute": "Calculating route...",
        "priceLabel": "Price:",
        "passengers": "Passengers",
        "baggage": "Need baggage?",
        "orderBtn": "Order Taxi",
        "loadingRoute": "Calculating route...",
        "selectPickupFirst": "Please select where you are departing from first!",
        "waitRouteCalc": "Route is still calculating, please wait...",
        "addressWaitOrMap": "Please wait for the address to resolve or select on map.",
        "selectAddressesFirst": "Please select both pickup and destination locations fully first!",
        "invalidCoords": "Invalid address coordinates! Please select locations on the map again.",
        "tariffs": {
            "start": "START",
            "startDesc": "Regular car",
            "komfort": "COMFORT",
            "komfortDesc": "Comfortable car",
            "biznes": "BUSINESS",
            "biznesDesc": "Premium"
        }
    },
    "delivery": {
        "title": "Delivery",
        "bannerTitle": "Mail and Parcel",
        "bannerSub": "Fast and reliable delivery service",
        "fromPlaceholder": "Mail from where?",
        "toPlaceholder": "Mail to where?",
        "tariffName": "DELIVERY",
        "tariffSub": "Mail delivery",
        "tariffRate": "100 sum/km",
        "orderBtn": "Request Delivery",
        "loadingRoute": "Calculating route...",
        "selectAddressesFirst": "Please select pickup and delivery locations for the parcel!"
    },
    "pickup": {
        "title": "Where are you?"
    },
    "dest": {
        "title": "Where are you going?"
    },
    "waiting": {
        "searchDriver": "Searching for driver...",
        "searchDesc": "Please wait. Your request has been sent to drivers."
    },
    "trip": {
        "statusActive": "Trip in progress",
        "statusFinished": "YOU ARRIVED",
        "statusDriverEnRoute": "DRIVER EN ROUTE",
        "statusError": "ERROR: ROUTE NOT FOUND",
        "driverName": "Driver",
        "carInfo": "Car",
        "cancelBtn": "Cancel Trip",
        "kmLeft": "km left",
        "mLeft": "m left"
    },
    "trips": {
        "title": "My Trips",
        "noTrips": "You have no trips yet.",
        "completed": "Completed",
        "active": "Active trip"
    },
    "messages": {
        "title": "Messages",
        "supportTitle": "Customer Support",
        "supportWelcome": "Hello, how can I help you today?",
        "promoTitle": "Deals & Discounts",
        "promoWelcome": "New Year discounts have started!",
        "driverOnWay": "On my way, will arrive in 5 minutes."
    },
    "profile": {
        "title": "Profile",
        "defaultName": "User",
        "settings": "Settings",
        "help": "Help",
        "logout": "Log out"
    },
    "settings": {
        "title": "Settings",
        "language": "Language / Til",
        "phoneUnchangeable": "Phone number (Cannot be changed)",
        "savedSuccess": "Changes saved!"
    },
    "call": {
        "status": "Calling..."
    },
    "chat": {
        "inputPlaceholder": "Type a message...",
        "online": "Online",
        "edit": "Edit",
        "delete": "Delete",
        "edited": "(edited)",
        "driverWelcome": "Hello! I have arrived, where are you?",
        "confirmDelete": "Do you want to delete this message?",
        "editPrompt": "Edit message:",
        "supportReply1": "Please leave your issue, we will reply shortly.",
        "supportReply2": "Our operators are busy right now, please wait.",
        "supportReply3": "Your message is received. An operator will contact you shortly.",
        "driverReply1": "Okay, understood.",
        "driverReply2": "I will be there shortly.",
        "driverReply3": "On my way.",
        "driverReply4": "Can you specify the address?"
    }
}
};

async function loadTranslations() {
    try {
        const [uzRes, ruRes, enRes] = await Promise.all([
            fetch('./uz.json').then(r => r.json()),
            fetch('./ru.json').then(r => r.json()),
            fetch('./en.json').then(r => r.json())
        ]);
        loadedTranslations.uz = uzRes;
        loadedTranslations.ru = ruRes;
        loadedTranslations.en = enRes;
    } catch (e) {
        console.error("Failed to load translation files", e);
    }
}

function getValueByPath(obj, path) {
    if (!obj || !path) return undefined;
    return path.split('.').reduce((acc, part) => acc && acc[part], obj);
}

function t(key, replacements = {}) {
    let val = getValueByPath(loadedTranslations[currentLang], key);
    if (val === undefined) {
        val = getValueByPath(loadedTranslations['uz'], key);
    }
    if (val === undefined) {
        val = getValueByPath(loadedTranslations['ru'], key);
    }
    if (val === undefined) {
        val = getValueByPath(loadedTranslations['en'], key);
    }
    if (val === undefined) {
        console.warn("Missing translation key: " + key);
        return "";
    }
    
    let str = String(val);
    Object.keys(replacements).forEach(k => {
        str = str.replace(new RegExp(`{${k}}`, 'g'), replacements[k]);
    });
    return str;
}

let currentLang = localStorage.getItem('vg_lang') || 'uz';

function setLanguage(lang) {
    currentLang = lang;
    localStorage.setItem('vg_lang', lang);
    updateUITranslations();
}

function updateUITranslations() {
    document.querySelectorAll('[data-i18n]').forEach(el => {
        const key = el.getAttribute('data-i18n');
        const translated = t(key);
        if (translated !== "") {
            el.innerHTML = translated;
        }
    });
    document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
        const key = el.getAttribute('data-i18n-placeholder');
        const translated = t(key);
        if (translated !== "") {
            el.placeholder = translated;
        }
    });
    
    const bagStatus = document.getElementById('baggage-status');
    if (bagStatus) {
        bagStatus.innerText = typeof needsBaggage !== "undefined" && needsBaggage ? t('common.yes') : t('common.no');
    }
    
    // Highlight login language card if it exists
    document.querySelectorAll('.lang-btn').forEach(btn => {
        btn.classList.remove('border-primary', 'bg-white', 'shadow-md');
        btn.classList.add('border-transparent', 'bg-bgLight');
        const checkIcon = btn.querySelector('.lang-check');
        if (checkIcon) checkIcon.classList.add('hidden');
    });
    const selectedLangCard = document.getElementById(`lang-${currentLang}`);
    if (selectedLangCard) {
        selectedLangCard.classList.remove('border-transparent', 'bg-bgLight');
        selectedLangCard.classList.add('border-primary', 'bg-white', 'shadow-md');
        const checkIcon = selectedLangCard.querySelector('.lang-check');
        if (checkIcon) checkIcon.classList.remove('hidden');
    }
    
    const settingsLang = document.getElementById('settings-language');
    if (settingsLang && settingsLang.value !== currentLang) settingsLang.value = currentLang;

    // Refresh dynamic states on language change
    const pickupList = document.getElementById('pickup-region-list');
    if (pickupList) renderRegions('pickup-region-list', 'pickup');
    const destList = document.getElementById('dest-region-list');
    if (destList) renderRegions('dest-region-list', 'dest');
    
    updateOrderButton();
    updateDeliveryPriceDisplay();
    renderProfil();
    renderSafarlarim();
    renderXabarlar();
    if (typeof currentChatId !== 'undefined' && currentChatId) renderMessages();

    // Update promo banner image source dynamically based on the current language
    const promoBanner = document.getElementById('home-promo-banner');
    if (promoBanner) {
        let bannerSrc = 'rasmlar/banner.png';
        if (currentLang === 'ru') {
            bannerSrc = 'rasmlar/bannerru.png';
        } else if (currentLang === 'en') {
            bannerSrc = 'rasmlar/bannereng.png';
        }
        promoBanner.src = bannerSrc;
        const bannerContainer = promoBanner.parentElement.parentElement;
        if (bannerContainer) {
            bannerContainer.style.display = '';
        }
    }
}

// --- Data Models ---
const regionsData = [
    { id: 'toshkent_sh', name: "Toshkent shahri", lat: 41.2995, lng: 69.2401 },
    { id: 'toshkent_v', name: "Toshkent viloyati", lat: 41.2000, lng: 69.2500 },
    { id: 'andijon', name: "Andijon", lat: 40.7821, lng: 72.3442 },
    { id: 'fargona', name: "Farg‘ona", lat: 40.3842, lng: 71.7843 },
    { id: 'namangan', name: "Namangan", lat: 41.0011, lng: 71.6683 },
    { id: 'samarqand', name: "Samarqand", lat: 39.6270, lng: 66.9750 },
    { id: 'buxoro', name: "Buxoro", lat: 39.7681, lng: 64.4556 },
    { id: 'navoiy', name: "Navoiy", lat: 40.0844, lng: 65.3792 },
    { id: 'qashqadaryo', name: "Qashqadaryo", lat: 38.8612, lng: 65.7847 },
    { id: 'surxondaryo', name: "Surxondaryo", lat: 37.2242, lng: 67.2783 },
    { id: 'jizzax', name: "Jizzax", lat: 40.1158, lng: 67.8422 },
    { id: 'sirdaryo', name: "Sirdaryo", lat: 40.4897, lng: 68.7842 },
    { id: 'xorazm', name: "Xorazm", lat: 41.5500, lng: 60.6333 },
    { id: 'qoraqalpogiston', name: "Qoraqalpog‘iston Respublikasi", lat: 42.4619, lng: 59.6166 }
];

const mockDrivers = [
    { name: "Akmal Y.", rating: 4.9, car: "Chevrolet Cobalt", plate: "01 234 ABC", img: "11" },
    { name: "Shoxrux S.", rating: 4.8, car: "Lacetti", plate: "01 567 DEF", img: "12" },
    { name: "Diyor M.", rating: 5.0, car: "Kia K5", plate: "01 890 GHI", img: "13" },
    { name: "Rustam B.", rating: 4.7, car: "Chevrolet Nexia 3", plate: "01 321 JKL", img: "14" }
];

// --- Global State ---
let currentScreen = 'splash-screen';
let mapsInitialized = false;
let currentChatId = null;
let activeOrderId = null;
let loginGender = null;
let settingsGender = 'male';
let paxCount = 1;
let needsBaggage = false;
let nearbyMarkers = [];
let selectedPickup = { region: null, latlng: null, address: '' };
let selectedDest = { region: null, latlng: null, address: '' };

// Centralized route state
let routeState = {
    ready: false,
    distance: null,
    duration: null,
    distanceText: '',
    durationText: '',
    priceText: '',
    routeObject: null
};

// --- Delivery Module (independent from Taxi) ---
let delPickup = { region: null, latlng: null, address: '' };
let delDest = { region: null, latlng: null, address: '' };
let delMap = null;
let delPickupMarker = null;
let delDestMarker = null;
let delRouteState = {
    ready: false,
    distance: null,
    distanceText: '',
    priceText: ''
};
const DEL_PRICE_PER_KM = 100;
const DEL_MIN_PRICE = 3000;

function startDeliveryPickupFlow() {
    // Reuse the existing pickup region screen but flag we are in delivery mode
    window._deliveryFlowMode = 'pickup';
    navigate('pickup-region-screen');
}

function startDeliveryDestFlow() {
    window._deliveryFlowMode = 'dest';
    navigate('dest-region-screen');
}

function calculateDeliveryPrice() {
    if (!delRouteState.ready || !delRouteState.distance) return null;
    const distanceKm = Math.max(1, Math.round(delRouteState.distance / 1000));
    const total = distanceKm * DEL_PRICE_PER_KM;
    return Math.max(total, DEL_MIN_PRICE);
}

function updateDeliveryPriceDisplay() {
    const infoCard = document.getElementById('del-info-card');
    const contentState = document.getElementById('del-content-state');
    const loadingState = document.getElementById('del-loading-state');
    const previewSection = document.getElementById('del-route-preview');
    const emptyState = document.getElementById('del-empty-state');
    const orderBtn = document.getElementById('del-order-btn');
    const priceEl = document.getElementById('del-price');

    const hasAddresses = delPickup.latlng && delDest.latlng;

    // 1. NO ADDRESSES
    if (!hasAddresses) {
        if (infoCard) infoCard.classList.add('hidden');
        if (previewSection) {
            previewSection.classList.remove('opacity-100', 'translate-y-0', 'pointer-events-auto');
            previewSection.classList.add('opacity-0', 'translate-y-10', 'pointer-events-none');
            setTimeout(() => {
                if (!delPickup.latlng || !delDest.latlng) previewSection.style.display = 'none';
            }, 500);
        }
        if (emptyState) {
            emptyState.style.display = 'flex';
            requestAnimationFrame(() => {
                emptyState.classList.remove('opacity-0', 'scale-95');
                emptyState.classList.add('opacity-100', 'scale-100');
            });
        }
        return;
    }

    // Hide empty state
    if (emptyState) {
        emptyState.classList.remove('opacity-100', 'scale-100');
        emptyState.classList.add('opacity-0', 'scale-95');
        setTimeout(() => {
            if (delPickup.latlng && delDest.latlng) emptyState.style.display = 'none';
        }, 500);
    }

    // Show preview
    if (previewSection && previewSection.style.display === 'none') {
        previewSection.style.display = 'flex';
        requestAnimationFrame(() => {
            requestAnimationFrame(() => {
                previewSection.classList.remove('opacity-0', 'translate-y-10', 'pointer-events-none');
                previewSection.classList.add('opacity-100', 'translate-y-0', 'pointer-events-auto');
                if (delMap) {
                    delMap.container.fitToViewport();
                    setTimeout(() => {
                        if (delMap) {
                            delMap.container.fitToViewport();
                            delMap.setZoom(delMap.getZoom());
                        }
                    }, 500);
                }
            });
        });
    }

    // 2. LOADING
    if (!delRouteState.ready || !delRouteState.distance) {
        if (infoCard) infoCard.classList.remove('hidden');
        if (contentState) contentState.classList.add('hidden');
        if (loadingState) loadingState.classList.remove('hidden');
        if (orderBtn) {
            orderBtn.disabled = true;
            orderBtn.classList.add('opacity-50', 'cursor-not-allowed');
            orderBtn.innerHTML = "<i class='fa-solid fa-box-open'></i> " + t('delivery.loadingRoute');
        }
        return;
    }

    // 3. READY
    if (infoCard) infoCard.classList.remove('hidden');
    if (loadingState) loadingState.classList.add('hidden');
    if (contentState) contentState.classList.remove('hidden');

    const distKm = (delRouteState.distance / 1000).toFixed(1) + " " + t('common.km');
    const distEl = document.getElementById('del-distance');
    if (distEl) distEl.innerText = distKm;

    const price = calculateDeliveryPrice();
    if (price !== null) {
        const priceFormatted = price.toLocaleString('ru-RU') + " " + t('common.currency');
        if (priceEl) priceEl.innerText = priceFormatted;
        delRouteState.priceText = priceFormatted;
    }

    if (orderBtn) {
        orderBtn.disabled = false;
        orderBtn.classList.remove('opacity-50', 'cursor-not-allowed');
        orderBtn.innerHTML = "<i class='fa-solid fa-box-open'></i> <span data-i18n='delivery.orderBtn'>" + t('delivery.orderBtn') + "</span>";
    }
}

function buildDeliveryRoute() {
    if (!delPickup.latlng || !delDest.latlng) return;

    delRouteState.ready = false;
    updateDeliveryPriceDisplay();

    ymaps.ready(() => {
        if (!delMap) {
            delMap = new ymaps.Map('del-map', {
                center: [41.2995, 69.2401], zoom: 12, controls: []
            }, { suppressMapOpenBlock: true, autoFitToViewport: 'always' });
        }

        delMap.geoObjects.removeAll();
        delMap.container.fitToViewport();

        const lat1 = Number(delPickup.latlng.lat);
        const lng1 = Number(delPickup.latlng.lng);
        const lat2 = Number(delDest.latlng.lat);
        const lng2 = Number(delDest.latlng.lng);

        // Pickup marker
        delMap.geoObjects.add(new ymaps.Placemark([lat1, lng1],
            { hintContent: delPickup.address },
            { preset: 'islands#yellowCircleDotIcon', zIndex: 500 }
        ));
        // Dest marker
        delMap.geoObjects.add(new ymaps.Placemark([lat2, lng2],
            { hintContent: delDest.address },
            { preset: 'islands#blackCircleDotIcon', zIndex: 500 }
        ));

        const osrmUrl = `https://router.project-osrm.org/route/v1/driving/${lng1},${lat1};${lng2},${lat2}?overview=full&geometries=polyline`;

        fetch(osrmUrl)
            .then(res => res.json())
            .then(data => {
                if (data.code !== 'Ok' || !data.routes || data.routes.length === 0) {
                    drawFallbackLine(delMap, lat1, lng1, lat2, lng2);
                    return;
                }
                const route = data.routes[0];
                const routeCoords = decodeOSRMPolyline(route.geometry);

                if (routeCoords.length > 0) {
                    delMap.geoObjects.add(new ymaps.Polyline(routeCoords, {}, {
                        strokeColor: '#00000044', strokeWidth: 10, strokeOpacity: 0.25, zIndex: 99
                    }));
                    delMap.geoObjects.add(new ymaps.Polyline(routeCoords, {}, {
                        strokeColor: '#111111', strokeWidth: 5, strokeOpacity: 1, zIndex: 100
                    }));
                }

                delRouteState.ready = true;
                delRouteState.distance = route.distance;
                delRouteState.distanceText = (route.distance / 1000).toFixed(0) + " " + t('common.km');

                updateDeliveryPriceDisplay();

                delMap.container.fitToViewport();
                const bounds = [
                    [Math.min(lat1, lat2) - 0.1, Math.min(lng1, lng2) - 0.1],
                    [Math.max(lat1, lat2) + 0.1, Math.max(lng1, lng2) + 0.1]
                ];
                delMap.setBounds(bounds, { checkZoomRange: true, zoomMargin: 60 });
                
                // Force tiles to load properly
                setTimeout(() => {
                    if (delMap) {
                        delMap.container.fitToViewport();
                        delMap.setZoom(delMap.getZoom());
                    }
                }, 500);
            })
            .catch(err => {
                console.error("Delivery route error:", err);
                drawFallbackLine(delMap, lat1, lng1, lat2, lng2);
            });
    });
}

function createDeliveryOrder() {
    if (!delPickup.latlng || !delDest.latlng || !delPickup.address || !delDest.address) {
        alert(t('delivery.selectAddressesFirst'));
        return;
    }
    if (!delRouteState.ready) {
        alert(t('taxi.waitRouteCalc'));
        return;
    }

    const order = {
        id: Date.now(),
        type: 'delivery',
        from: { name: delPickup.address, lat: Number(delPickup.latlng.lat), lng: Number(delPickup.latlng.lng) },
        to: { name: delDest.address, lat: Number(delDest.latlng.lat), lng: Number(delDest.latlng.lng) },
        distance: delRouteState.distanceText,
        price: delRouteState.priceText,
        status: 'pending',
        date: new Date().toISOString()
    };

    const activeOrders = JSON.parse(localStorage.getItem(DB_ACTIVE_ORDERS) || '[]');
    activeOrders.push(order);
    localStorage.setItem(DB_ACTIVE_ORDERS, JSON.stringify(activeOrders));

    navigate('waiting-screen');

    setTimeout(() => {
        simulateDriverAcceptance(order);
    }, 4000 + Math.random() * 2000);
}


// --- Pricing Engine ---
const TARIFFS = {
    start: { id: 'start', pricePerKm: 500, baggage: 5000 },
    komfort: { id: 'komfort', pricePerKm: 700, baggage: 10000 },
    biznes: { id: 'biznes', pricePerKm: 1000, baggage: 15000 }
};
let selectedTariff = 'start';

function selectTariff(tariffId) {
    selectedTariff = tariffId;
    
    // Reset styles
    const cards = document.querySelectorAll('.tariff-card');
    cards.forEach(card => {
        card.classList.remove('border-[#FFD400]');
        card.classList.add('border-transparent');
    });
    
    // Set active styles
    const activeCard = document.getElementById('tariff-' + tariffId);
    if(activeCard) {
        activeCard.classList.remove('border-transparent');
        activeCard.classList.add('border-[#FFD400]');
    }
    
    // Calculate and update prices in cards
    updatePriceDisplay();
}

function calculatePrice(specificTariff = null) {
    if (!routeState.ready || !routeState.distance) return null;
    const distanceKm = Math.round(routeState.distance / 1000);
    const tariffToUse = specificTariff ? specificTariff : selectedTariff;
    
    let baggageCost = needsBaggage ? TARIFFS[tariffToUse].baggage : 0;
    
    // distance * pricePerKm * passengers + baggage
    const total = (distanceKm * TARIFFS[tariffToUse].pricePerKm + baggageCost) * paxCount;
    return Math.round(total);
}

function updatePriceDisplay() {
    const infoCard = document.getElementById('preview-info-card');
    const contentState = document.getElementById('preview-content-state');
    const loadingState = document.getElementById('preview-loading-state');
    const previewSection = document.getElementById('route-preview-section');
    const mapPreviewContainer = document.getElementById('taxi-map-preview-container');
    const stickyBottomBar = document.getElementById('taxi-sticky-bottom-bar');
    const emptyState = document.getElementById('home-empty-state');
    const orderBtn = document.getElementById('order-btn');
    
    const hasAddresses = selectedPickup.latlng && selectedDest.latlng;

    // 1. NO ADDRESSES YET
    if (!hasAddresses) {
        if (infoCard) infoCard.classList.add('hidden');
        if (previewSection) {
            previewSection.classList.remove('opacity-100', 'translate-y-0', 'pointer-events-auto');
            previewSection.classList.add('opacity-0', 'translate-y-10', 'pointer-events-none');
            setTimeout(() => {
                if (!selectedPickup.latlng || !selectedDest.latlng) previewSection.style.display = 'none';
            }, 500);
        }
        if (mapPreviewContainer) {
            mapPreviewContainer.classList.remove('opacity-100', 'translate-y-0', 'pointer-events-auto');
            mapPreviewContainer.classList.add('opacity-0', 'translate-y-10', 'pointer-events-none');
            setTimeout(() => {
                if (!selectedPickup.latlng || !selectedDest.latlng) mapPreviewContainer.style.display = 'none';
            }, 500);
        }
        if (stickyBottomBar) {
            stickyBottomBar.classList.remove('opacity-100', 'translate-y-0', 'pointer-events-auto');
            stickyBottomBar.classList.add('opacity-0', 'translate-y-10', 'pointer-events-none');
            setTimeout(() => {
                if (!selectedPickup.latlng || !selectedDest.latlng) stickyBottomBar.style.display = 'none';
            }, 500);
        }
        if (emptyState) {
            emptyState.style.display = 'flex';
            requestAnimationFrame(() => {
                emptyState.classList.remove('opacity-0', 'scale-95');
                emptyState.classList.add('opacity-100', 'scale-100');
            });
        }
        return;
    }
    
    // BOTH ADDRESSES PRESENT (Either loading or ready)
    
    // Hide empty state
    if (emptyState) {
        emptyState.classList.remove('opacity-100', 'scale-100');
        emptyState.classList.add('opacity-0', 'scale-95');
        setTimeout(() => {
            if (selectedPickup.latlng && selectedDest.latlng) emptyState.style.display = 'none';
        }, 500);
    }
    
    // Show map container
    if (mapPreviewContainer && mapPreviewContainer.style.display === 'none') {
        mapPreviewContainer.style.display = 'block';
        requestAnimationFrame(() => {
            requestAnimationFrame(() => {
                mapPreviewContainer.classList.remove('opacity-0', 'translate-y-10', 'pointer-events-none');
                mapPreviewContainer.classList.add('opacity-100', 'translate-y-0', 'pointer-events-auto');
            });
        });
    }
    
    // Show sticky bottom bar
    if (stickyBottomBar && stickyBottomBar.style.display === 'none') {
        stickyBottomBar.style.display = 'block';
        requestAnimationFrame(() => {
            requestAnimationFrame(() => {
                stickyBottomBar.classList.remove('opacity-0', 'translate-y-10', 'pointer-events-none');
                stickyBottomBar.classList.add('opacity-100', 'translate-y-0', 'pointer-events-auto');
            });
        });
    }
    
    // Show preview section
    if (previewSection && previewSection.style.display === 'none') {
        previewSection.style.display = 'flex';
        requestAnimationFrame(() => {
            requestAnimationFrame(() => {
                previewSection.classList.remove('opacity-0', 'translate-y-10', 'pointer-events-none');
                previewSection.classList.add('opacity-100', 'translate-y-0', 'pointer-events-auto');
                if (YandexTrackingEngine.homeMap) YandexTrackingEngine.homeMap.container.fitToViewport();
            });
        });
    }

    // 2. LOADING STATE
    if (!routeState.ready || !routeState.distance) {
        if (infoCard) infoCard.classList.remove('hidden');
        if (contentState) contentState.classList.add('hidden');
        if (loadingState) loadingState.classList.remove('hidden');
        
        if (orderBtn) {
            orderBtn.disabled = true;
            orderBtn.classList.add('opacity-50', 'cursor-not-allowed');
            orderBtn.classList.remove('shadow-lg', 'shadow-yellow-500/20');
            orderBtn.innerText = t('taxi.loadingRoute');
        }
        return;
    }
    
    // 3. READY STATE
    if (infoCard) infoCard.classList.remove('hidden');
    if (loadingState) loadingState.classList.add('hidden');
    if (contentState) contentState.classList.remove('hidden');
    
    const distanceKm = (routeState.distance / 1000).toFixed(1) + " " + t('common.km');
    const distEl = document.getElementById('preview-distance');
    if (distEl) distEl.innerText = distanceKm;
    
    // Update all tariff cards realtime
    Object.keys(TARIFFS).forEach(tariff => {
        const tPrice = calculatePrice(tariff);
        const tPriceEl = document.getElementById('price-' + tariff);
        if(tPriceEl && tPrice !== null) {
            tPriceEl.innerText = tPrice.toLocaleString('ru-RU') + " " + t('common.currency');
        }
    });

    const activePrice = calculatePrice();
    if(activePrice !== null) {
        routeState.priceText = activePrice.toLocaleString('ru-RU') + " " + t('common.currency');
    }
    
    if (orderBtn) {
        orderBtn.disabled = false;
        orderBtn.classList.remove('opacity-50', 'cursor-not-allowed');
        orderBtn.classList.add('shadow-lg', 'shadow-yellow-500/20');
        orderBtn.innerText = t('taxi.orderBtn');
    }
}

// Global Map Instances
let pickupMap = null;
let destMap = null;
let homeMap = null;
let tripMap = null;

// Global Markers
let pickupMarker = null;
let destMarker = null;

// --- Storage Keys ---
const DB_USER = 'vg_currentUser';
const DB_ACTIVE_ORDERS = 'vg_activeOrders';
const DB_COMPLETED_ORDERS = 'vg_completedOrders';
const DB_SKIP_ONBOARDING = 'vg_routeOnboardingSkipped';
const DB_CHATS = 'vg_chats';

// --- Yandex Tracking Engine (Isolated Module) ---
const YandexTrackingEngine = {
    homeMap: null,
    tripMap: null,
    activeRoute: null,
    taxiMarker: null,
    interval: null,
    layoutReady: false,
    initInProgress: false,

    async ensureReady() {
        if (typeof ymaps === 'undefined') {
            console.warn("DEBUG: ymaps undefined. Waiting for API script...");
            // Wait up to 5 seconds for ymaps to appear
            for (let i = 0; i < 50; i++) {
                await new Promise(r => setTimeout(r, 100));
                if (typeof ymaps !== 'undefined') break;
            }
            if (typeof ymaps === 'undefined') {
                console.error("DEBUG: Yandex Maps API failed to load after 5s.");
                return false;
            }
        }

        if (this.initInProgress) {
            // Wait for existing init to finish
            while (this.initInProgress) {
                await new Promise(r => setTimeout(r, 100));
            }
            return true;
        }

        this.initInProgress = true;
        console.log("DEBUG: Yandex Engine initializing...");

        return new Promise((resolve) => {
            ymaps.ready(() => {
                console.log("DEBUG: ymaps.ready() fired.");
                this.registerLayouts();
                this.initInProgress = false;
                resolve(true);
            });
        });
    },

    registerLayouts() {
        if (this.layoutReady || typeof ymaps === 'undefined') return;
        
        try {
            const CarIconLayout = ymaps.templateLayoutFactory.createClass(
                '<div class="car-marker-wrapper" style="transform: rotate($[properties.iconRotate]deg); transition: transform 0.4s ease-in-out;">' +
                '<img src="rasmlar/car.png" style="width: 44px; height: 44px; filter: drop-shadow(0 4px 8px rgba(0,0,0,0.3));">' +
                '</div>'
            );
            ymaps.layout.storage.add('custom#taxiLayout', CarIconLayout);
            this.layoutReady = true;
            console.log("DEBUG: Yandex Layouts registered.");

            const img = new Image();
            img.src = "rasmlar/car.png";
        } catch (e) {
            console.error("DEBUG: Layout registration failed:", e);
        }
    },

    initHomeMap() {
        if (this.homeMap) return;
        const container = document.getElementById('home-map');
        if (!container || container.clientHeight === 0) {
            console.warn("DEBUG: home-map container not ready or hidden. Deferring init...");
            return;
        }
        console.log("DEBUG: Creating homeMap instance...");
        this.homeMap = new ymaps.Map('home-map', {
            center: [41.2995, 69.2401], zoom: 12, controls: []
        }, { 
            suppressMapOpenBlock: true, 
            yandexMapDisablePoiInteractivity: true,
            autoFitToViewport: 'always'
        });
        
        // Multiple fit attempts to force tiles
        setTimeout(() => { 
            if (this.homeMap) {
                this.homeMap.container.fitToViewport();
                this.homeMap.setZoom(this.homeMap.getZoom()); // Force redraw
            }
        }, 500);
        setTimeout(() => { if (this.homeMap) this.homeMap.container.fitToViewport(); }, 1500);
    },

    async showHomeMap() {
        await this.ensureReady();
        requestAnimationFrame(() => {
            requestAnimationFrame(() => {
                this.initHomeMap();
                if (this.homeMap) {
                    this.homeMap.container.fitToViewport();
                }
            });
        });
    },

    initTripMap() {
        if (this.tripMap) return;
        const container = document.getElementById('trip-map');
        if (!container || container.clientHeight === 0) {
            console.warn("DEBUG: trip-map container not ready or hidden. Deferring init...");
            return;
        }
        console.log("DEBUG: Creating tripMap instance...");
        this.tripMap = new ymaps.Map('trip-map', {
            center: [41.2995, 69.2401], zoom: 8, controls: []
        }, { suppressMapOpenBlock: true });
    },

    async buildHomeRoute() {
        const ready = await this.ensureReady();
        if (!ready) return;

        if (!selectedPickup.latlng || !selectedDest.latlng) {
            console.warn("DEBUG: buildHomeRoute called without pickup/dest");
            return;
        }

        // Reset route state
        routeState.ready = false;
        updateOrderButton();

        const self = this;
        const tryBuild = (attempt) => {
            self.initHomeMap();
            if (!self.homeMap) {
                if (attempt < 20) {
                    setTimeout(() => tryBuild(attempt + 1), 300);
                }
                return;
            }

            // Clear everything
            self.homeMap.geoObjects.removeAll();
            self.homeMap.container.fitToViewport();

            const lat1 = Number(selectedPickup.latlng.lat);
            const lng1 = Number(selectedPickup.latlng.lng);
            const lat2 = Number(selectedDest.latlng.lat);
            const lng2 = Number(selectedDest.latlng.lng);

            if (isNaN(lat1) || isNaN(lng1) || isNaN(lat2) || isNaN(lng2)) {
                console.error("DEBUG: Invalid coordinates", { lat1, lng1, lat2, lng2 });
                return;
            }

            console.log("DEBUG: Building route via OSRM from", [lat1, lng1], "to", [lat2, lng2]);

            // Add pickup marker (yellow)
            self.homeMap.geoObjects.add(new ymaps.Placemark([lat1, lng1],
                { hintContent: selectedPickup.address },
                { preset: 'islands#yellowCircleDotIcon', zIndex: 500 }
            ));

            // Add destination marker (black)
            self.homeMap.geoObjects.add(new ymaps.Placemark([lat2, lng2],
                { hintContent: selectedDest.address },
                { preset: 'islands#blackCircleDotIcon', zIndex: 500 }
            ));

            // Use OSRM free routing API (no API key needed)
            const osrmUrl = `https://router.project-osrm.org/route/v1/driving/${lng1},${lat1};${lng2},${lat2}?overview=full&geometries=polyline`;

            fetch(osrmUrl)
                .then(res => res.json())
                .then(data => {
                    if (data.code !== 'Ok' || !data.routes || data.routes.length === 0) {
                        console.error("DEBUG: OSRM returned no routes", data);
                        drawFallbackLine(self.homeMap, lat1, lng1, lat2, lng2);
                        return;
                    }

                    const route = data.routes[0];
                    const distanceKm = (route.distance / 1000).toFixed(0);
                    const durationMin = Math.round(route.duration / 60);
                    const hours = Math.floor(durationMin / 60);
                    const mins = durationMin % 60;

                    const distText = distanceKm + " " + t('common.km');

                    // Decode polyline geometry from OSRM
                    const routeCoords = decodeOSRMPolyline(route.geometry);

                    console.log("DEBUG: OSRM route:", distText, routeCoords.length, "points");

                    if (routeCoords.length > 0) {
                        // Draw shadow line
                        const shadowLine = new ymaps.Polyline(routeCoords, {}, {
                            strokeColor: '#00000044',
                            strokeWidth: 10,
                            strokeOpacity: 0.25,
                            zIndex: 99
                        });
                        self.homeMap.geoObjects.add(shadowLine);

                        // Draw the main black route line
                        const routeLine = new ymaps.Polyline(routeCoords, {
                            hintContent: distText
                        }, {
                            strokeColor: '#111111',
                            strokeWidth: 5,
                            strokeOpacity: 1,
                            zIndex: 100
                        });
                        self.homeMap.geoObjects.add(routeLine);
                    }

                    // Store route state
                    routeState.ready = true;
                    routeState.distance = route.distance;
                    routeState.duration = route.duration;
                    routeState.distanceText = distText;
                    routeState.durationText = '';
                    routeState.routeObject = null;

                    // Enable button
                    updateOrderButton();
                    updatePriceDisplay();

                    // Fit map to route
                    self.homeMap.container.fitToViewport();
                    const bounds = [
                        [Math.min(lat1, lat2) - 0.1, Math.min(lng1, lng2) - 0.1],
                        [Math.max(lat1, lat2) + 0.1, Math.max(lng1, lng2) + 0.1]
                    ];
                    self.homeMap.setBounds(bounds, {
                        checkZoomRange: true,
                        zoomMargin: 60
                    });
                })
                .catch(err => {
                    console.error("DEBUG: OSRM fetch error:", err);
                    drawFallbackLine(self.homeMap, lat1, lng1, lat2, lng2);
                });
        };

        requestAnimationFrame(() => {
            requestAnimationFrame(() => tryBuild(0));
        });
    },

    async startTripTracking(order) {
        const ready = await this.ensureReady();
        if (!ready) return;

        this.initTripMap();
        if (!this.tripMap) return;
        
        globalLastRotation = null;

        console.log("DEBUG: Starting trip tracking...");
        if (this.interval) clearInterval(this.interval);
        if (this._animFrame) cancelAnimationFrame(this._animFrame);
        this.tripMap.geoObjects.removeAll();
        this.tripMap.container.fitToViewport();

        const pLat = Number(order.from.lat);
        const pLng = Number(order.from.lng);
        const dLat = Number(order.to.lat);
        const dLng = Number(order.to.lng);

        const fromCoords = [pLat, pLng];
        const toCoords = [dLat, dLng];
        const driverStartCoords = [
            pLat + (Math.random() > 0.5 ? 0.005 : -0.005),
            pLng + (Math.random() > 0.5 ? 0.005 : -0.005)
        ];

        // Add pickup and destination markers
        this.tripMap.geoObjects.add(new ymaps.Placemark(fromCoords,
            { hintContent: "Olib ketish" },
            { preset: 'islands#yellowCircleDotIcon', zIndex: 200 }
        ));
        this.tripMap.geoObjects.add(new ymaps.Placemark(toCoords,
            { hintContent: "Borish manzili" },
            { preset: 'islands#blackCircleDotIcon', zIndex: 200 }
        ));

        // Fetch route from OSRM
        const osrmUrl = `https://router.project-osrm.org/route/v1/driving/${driverStartCoords[1]},${driverStartCoords[0]};${pLng},${pLat};${dLng},${dLat}?overview=full&geometries=polyline`;
        const self = this;

        fetch(osrmUrl)
            .then(res => res.json())
            .then(data => {
                if (data.code !== 'Ok' || !data.routes || data.routes.length === 0) {
                    document.getElementById('trip-status').innerText = t('trip.statusError');
                    return;
                }

                const route = data.routes[0];
                const totalDistance = route.distance; // meters
                const totalDuration = route.duration; // seconds
                const allPoints = decodeOSRMPolyline(route.geometry);

                if (allPoints.length < 2) return;

                console.log("DEBUG: Trip route:", allPoints.length, "points,",
                    (totalDistance/1000).toFixed(1), "km,",
                    Math.round(totalDuration/60), "min");

                // --- Pre-calculate cumulative distances for accurate progress ---
                const segDistances = [0]; // cumulative distance at each point
                let cumulDist = 0;
                for (let i = 1; i < allPoints.length; i++) {
                    cumulDist += haversineMeters(allPoints[i-1], allPoints[i]);
                    segDistances.push(cumulDist);
                }
                const routeTotalDist = cumulDist; // actual geometry distance

                // --- Create COMPLETED route polyline (gray, behind) ---
                const completedLine = new ymaps.Polyline([], {}, {
                    strokeColor: '#BBBBBB',
                    strokeWidth: 5,
                    strokeOpacity: 0.6,
                    zIndex: 90
                });
                self.tripMap.geoObjects.add(completedLine);

                // --- Create REMAINING route polyline (black, on top) ---
                const remainingLine = new ymaps.Polyline(allPoints, {}, {
                    strokeColor: '#111111',
                    strokeWidth: 5,
                    strokeOpacity: 1,
                    zIndex: 100
                });
                self.tripMap.geoObjects.add(remainingLine);

                // --- Create taxi marker with proper offset for accurate positioning ---
                self.taxiMarker = new ymaps.Placemark(allPoints[0], {}, {
                    iconLayout: 'custom#taxiLayout',
                    iconOffset: [-22, -22],
                    iconShape: { type: 'Circle', coordinates: [22, 22], radius: 22 },
                    iconPane: 'overlaps',
                    zIndex: 3000
                });
                self.tripMap.geoObjects.add(self.taxiMarker);

                // --- Initial map fit ---
                self.tripMap.setBounds([
                    [Math.min(pLat, dLat) - 0.02, Math.min(pLng, dLng) - 0.02],
                    [Math.max(pLat, dLat) + 0.02, Math.max(pLng, dLng) + 0.02]
                ], { checkZoomRange: true, zoomMargin: 80 });

                // --- Update initial distance ---
                updateTripInfo(totalDistance);

                // --- Animation State ---
                const SPEED_MULTIPLIER = 15; // x15 real speed for simulation
                const startTime = performance.now();
                const simDuration = (totalDuration / SPEED_MULTIPLIER) * 1000; // ms
                let lastMapPan = 0;

                // --- Smooth animation loop ---
                function animateStep(now) {
                    const elapsed = now - startTime;
                    const progress = Math.min(elapsed / simDuration, 1.0);

                    // Find the current distance traveled
                    const distTraveled = progress * routeTotalDist;

                    // Find which segment we're on via cumulative distances
                    let segIdx = 0;
                    for (let i = 1; i < segDistances.length; i++) {
                        if (segDistances[i] >= distTraveled) {
                            segIdx = i - 1;
                            break;
                        }
                        if (i === segDistances.length - 1) segIdx = i - 1;
                    }

                    // Interpolate position within the segment
                    const segStart = segDistances[segIdx];
                    const segEnd = segDistances[segIdx + 1] || segStart + 1;
                    const segProgress = Math.min((distTraveled - segStart) / (segEnd - segStart), 1);

                    const p1 = allPoints[segIdx];
                    const p2 = allPoints[segIdx + 1] || allPoints[segIdx];
                    const currentPos = [
                        p1[0] + (p2[0] - p1[0]) * segProgress,
                        p1[1] + (p2[1] - p1[1]) * segProgress
                    ];

                    // Move taxi
                    self.taxiMarker.geometry.setCoordinates(currentPos);

                    // Rotate taxi in movement direction
                    const rotation = calculateRotation(p1, p2);
                    self.taxiMarker.properties.set('iconRotate', rotation);

                    // Update polylines: completed = gray, remaining = black
                    const completedCoords = allPoints.slice(0, segIdx + 1).concat([currentPos]);
                    const remainingCoords = [currentPos].concat(allPoints.slice(segIdx + 1));

                    completedLine.geometry.setCoordinates(completedCoords);
                    remainingLine.geometry.setCoordinates(remainingCoords);

                    // Update distance display
                    const remainingFraction = 1 - progress;
                    const remainingDist = totalDistance * remainingFraction;
                    updateTripInfo(remainingDist);

                    // Gently pan map to follow taxi every 3 seconds
                    if (now - lastMapPan > 3000) {
                        lastMapPan = now;
                        self.tripMap.panTo(currentPos, { flying: false, duration: 1000 });
                    }

                    // Continue or finish
                    if (progress < 1.0) {
                        self._animFrame = requestAnimationFrame(animateStep);
                    } else {
                        // Trip complete
                        self.taxiMarker.geometry.setCoordinates(allPoints[allPoints.length - 1]);
                        completedLine.geometry.setCoordinates(allPoints);
                        remainingLine.geometry.setCoordinates([]);

                        document.getElementById('trip-status').innerText = t('trip.statusFinished');
                        document.getElementById('trip-distance').innerText = "0 " + t('common.km');

                        // Zoom to destination
                        self.tripMap.setCenter(toCoords, 15, { duration: 1000 });
                    }
                }

                // Start animation
                self._animFrame = requestAnimationFrame(animateStep);
            })
            .catch(err => {
                console.error("DEBUG: Trip OSRM error:", err);
                document.getElementById('trip-status').innerText = t('trip.statusError');
            });

        // Final fit
        setTimeout(() => { if (this.tripMap) this.tripMap.container.fitToViewport(); }, 500);
    }
};

// --- Initialization ---
document.addEventListener('DOMContentLoaded', async () => {
    await loadTranslations();
    initInteractions();
    updateUITranslations();
    setLoginGender(null);
    navigate('splash-screen', false);
    
    YandexTrackingEngine.ensureReady().then(() => {
        console.log("DEBUG: Yandex Engine Ready");
    });
});

// --- Navigation ---
let isNavigating = false;

function navigate(screenId, animate = true) {
    // Prevent rapid double-clicks causing flicker
    if (isNavigating && animate) return;
    
    const screens = document.querySelectorAll('.screen');
    const bottomNav = document.getElementById('bottom-nav');
    const targetScreen = document.getElementById(screenId);
    
    if (!targetScreen) return;

    // Lock navigation during transition
    if (animate) {
        isNavigating = true;
        setTimeout(() => { isNavigating = false; }, 350);
    }

    // Update Bottom Nav Active State
    document.querySelectorAll('.nav-btn').forEach(btn => {
        btn.classList.remove('text-primary');
        btn.classList.add('text-textSecondary');
    });
    const activeBtn = document.getElementById(`nav-btn-${screenId}`);
    if (activeBtn) {
        activeBtn.classList.remove('text-textSecondary');
        activeBtn.classList.add('text-primary');
    }

    // Hide ALL screens except the target
    screens.forEach(screen => {
        if (screen.id === screenId) return; // skip target
        
        screen.style.pointerEvents = 'none';
        if (screen.id === currentScreen && animate) {
            // The currently visible screen: animate it out
            screen.style.transition = 'transform 0.3s ease';
            screen.style.transform = 'translateX(-100%)';
            // After animation, move it far off and hide it
            setTimeout(() => {
                if (screen.id !== currentScreen) {
                    screen.style.transition = 'none';
                    screen.style.transform = 'translateX(100%)';
                    screen.style.visibility = 'hidden';
                }
            }, 320);
        } else {
            // All other screens: instantly hide (no animation)
            screen.style.transition = 'none';
            screen.style.transform = 'translateX(100%)';
            screen.style.visibility = 'hidden';
        }
    });

    // Show target screen
    targetScreen.style.visibility = 'visible';
    targetScreen.style.pointerEvents = 'auto';
    if (animate) {
        targetScreen.style.transition = 'transform 0.3s ease';
    } else {
        targetScreen.style.transition = 'none';
    }
    targetScreen.style.transform = 'translateX(0)';
    currentScreen = screenId;

    // Bottom nav visibility
    if (['home-screen', 'safarlarim-screen', 'xabarlar-screen', 'profil-screen', 'pickup-region-screen'].includes(screenId)) {
        if (bottomNav) {
            bottomNav.style.display = 'flex';
            bottomNav.style.pointerEvents = 'auto';
        }
        // highlight home if we are in pickup/dest flow
        if (['pickup-region-screen', 'pickup-map-screen', 'dest-region-screen', 'dest-map-screen'].includes(screenId)) {
            const homeBtn = document.getElementById('nav-btn-home-screen');
            if (homeBtn) {
                homeBtn.classList.remove('text-textSecondary');
                homeBtn.classList.add('text-primary');
            }
        }
        
        // Trigger resize/fit for engine-managed maps
        setTimeout(() => {
            if (screenId === 'active-trip-screen' && YandexTrackingEngine.tripMap) {
                YandexTrackingEngine.tripMap.container.fitToViewport();
            }
        }, 350);
        
        // Refresh dynamic screens
        if (screenId === 'safarlarim-screen') renderSafarlarim();
        if (screenId === 'xabarlar-screen') renderXabarlar();
        if (screenId === 'profil-screen') renderProfil();
    } else {
        if (bottomNav) bottomNav.style.display = 'none';
        
        // Handle maps on non-nav screens
        if (['pickup-map-screen', 'dest-map-screen', 'active-trip-screen', 'taxi-screen', 'delivery-screen'].includes(screenId)) {
            setTimeout(() => {
                if (screenId === 'taxi-screen') {
                    YandexTrackingEngine.showHomeMap();
                    if (YandexTrackingEngine.homeMap) YandexTrackingEngine.homeMap.container.fitToViewport();
                    // Rebuild route if both addresses exist but route not ready
                    if (selectedPickup.latlng && selectedDest.latlng && !routeState.ready) {
                        setTimeout(() => drawHomeRoute(), 500);
                    }
                    updateOrderButton();
                    updatePriceDisplay();
                }
                if (screenId === 'delivery-screen' && delMap) {
                    delMap.container.fitToViewport();
                    updateDeliveryPriceDisplay();
                }
                if (screenId === 'pickup-map-screen' && pickupMap) {
                    pickupMap.container.fitToViewport();
                    const center = pickupMap.getCenter();
                    pickupMap.setCenter(center, pickupMap.getZoom());
                    setTimeout(() => { if (pickupMap) pickupMap.container.fitToViewport(); }, 500);
                }
                if (screenId === 'dest-map-screen' && destMap) {
                    destMap.container.fitToViewport();
                    const center = destMap.getCenter();
                    destMap.setCenter(center, destMap.getZoom());
                    setTimeout(() => { if (destMap) destMap.container.fitToViewport(); }, 500);
                }
                if (screenId === 'active-trip-screen' && tripMap) {
                    tripMap.container.fitToViewport();
                    const center = tripMap.getCenter();
                    tripMap.setCenter(center, tripMap.getZoom());
                    setTimeout(() => { if (tripMap) tripMap.container.fitToViewport(); }, 500);
                }
            }, 400);
        }
    }
}

function startApp() {
    console.log("startApp called");
    try {
        const user = localStorage.getItem(DB_USER);
        if (user) {
            console.log("User found, navigating to home-screen");
            navigate('home-screen');
        } else {
            console.log("No user found, navigating to login-step-1");
            navigate('login-step-1');
        }
    } catch (e) {
        console.error("startApp error:", e);
        navigate('login-step-1');
    }
}

// --- Login & Logout ---
function handleStartJourney() {
    const isLoggedIn = localStorage.getItem(DB_USER);

    if (isLoggedIn) {
        navigate('home-screen');
    } else {
        navigate('login-step-1');
    }
}

function setLoginGender(gender) {
    loginGender = gender;
    document.querySelectorAll('.gender-btn').forEach(btn => {
        btn.classList.remove('border-primary', 'bg-white', 'shadow-md');
        btn.classList.add('border-transparent', 'bg-bgLight');
        const check = btn.querySelector('.gender-check');
        if (check) check.classList.add('hidden');
    });
    if (gender) {
        const selected = document.getElementById(`gender-${gender}`);
        if (selected) {
            selected.classList.remove('border-transparent', 'bg-bgLight');
            selected.classList.add('border-primary', 'bg-white', 'shadow-md');
            const check = selected.querySelector('.gender-check');
            if (check) check.classList.remove('hidden');
        }
    }
    const genderError = document.getElementById('login-gender-error');
    if (genderError) genderError.classList.add('hidden');
    
    // Automatically proceed to next step
    if (gender) {
        setTimeout(() => {
            validateStep3();
        }, 200);
    }
}

function setLoginLanguage(lang) {
    setLanguage(lang);
    // Automatically submit login
    setTimeout(() => {
        handleLogin();
    }, 200);
}

function validateStep1() {
    const phoneInput = document.getElementById('login-phone');
    const phoneError = document.getElementById('login-phone-error');
    if (!phoneInput) return;
    
    const phone = phoneInput.value;
    const digits = phone.replace(/[^\d]/g, '');
    
    if (digits.length !== 12) {
        phoneInput.classList.add('border-red-500', 'ring-2', 'ring-red-200');
        if (phoneError) {
            phoneError.innerText = t('login.phoneError');
            phoneError.classList.remove('hidden');
        }
        return;
    }
    
    phoneInput.classList.remove('border-red-500', 'ring-2', 'ring-red-200');
    if (phoneError) phoneError.classList.add('hidden');
    navigate('login-step-2');
}

function validateStep2() {
    const nameInput = document.getElementById('login-name');
    const nameError = document.getElementById('login-name-error');
    if (!nameInput) return;
    
    const name = nameInput.value.trim();
    if (!name || name.length < 2) {
        nameInput.classList.add('border-red-500', 'ring-2', 'ring-red-200');
        if (nameError) {
            nameError.innerText = t('login.nameError');
            nameError.classList.remove('hidden');
        }
        return;
    }
    
    nameInput.classList.remove('border-red-500', 'ring-2', 'ring-red-200');
    if (nameError) nameError.classList.add('hidden');
    navigate('login-step-3');
}

function validateStep3() {
    const genderError = document.getElementById('login-gender-error');
    if (!loginGender) {
        if (genderError) {
            genderError.innerText = t('login.genderError');
            genderError.classList.remove('hidden');
        }
        return;
    }
    if (genderError) genderError.classList.add('hidden');
    navigate('login-step-4');
}

function handleLogin() {
    const nameInput = document.getElementById('login-name');
    const surnameInput = document.getElementById('login-surname');
    const phoneInput = document.getElementById('login-phone');
    
    if (!nameInput || !surnameInput || !phoneInput) {
        console.error("Login inputs not found");
        return;
    }

    const name = nameInput.value.trim();
    const surname = surnameInput.value.trim();
    const phone = phoneInput.value.trim();

    // 1. Validation
    const digits = phone.replace(/[^\d]/g, '');
    if (digits.length !== 12) {
        alert(t('login.phoneError'));
        return;
    }
    if (!name || name.length < 2) {
        alert(t('login.nameError'));
        return;
    }
    if (!loginGender) {
        alert(t('login.genderError'));
        return;
    }

    const cleanedPhone = '+' + digits;

    // 2. Save User Data
    const userData = {
        name: name,
        surname: surname,
        phone: cleanedPhone,
        gender: loginGender,
        avatar: loginGender === 'female' ? 'rasmlar/girlavatar.png' : 'rasmlar/boyavatar.png',
        registeredAt: new Date().toISOString()
    };

    localStorage.setItem(DB_USER, JSON.stringify(userData));
    console.log("User registered successfully:", userData);

    // 3. Navigate
    navigate('home-screen');
    
    // Refresh profile UI if needed
    if (typeof renderProfil === 'function') renderProfil();
}

function logout() {
    localStorage.removeItem(DB_USER);
    navigate('splash-screen');
}

// --- UI Interactions ---
function initInteractions() {
    const phoneInput = document.getElementById('login-phone');
    if (phoneInput) {
        phoneInput.addEventListener('input', (e) => {
            let val = phoneInput.value;
            if (!val.startsWith('+')) {
                val = '+' + val.replace(/[^\d]/g, '');
            }
            let digits = val.replace(/[^\d]/g, '');
            if (!digits.startsWith('998')) {
                digits = '998' + digits;
            }
            digits = digits.slice(0, 12);
            let formatted = '+998';
            if (digits.length > 3) {
                formatted += ' (' + digits.substring(3, 5);
            }
            if (digits.length > 5) {
                formatted += ') ' + digits.substring(5, 8);
            }
            if (digits.length > 8) {
                formatted += '-' + digits.substring(8, 10);
            }
            if (digits.length > 10) {
                formatted += '-' + digits.substring(10, 12);
            }
            phoneInput.value = formatted;
            const phoneError = document.getElementById('login-phone-error');
            if (digits.length === 12) {
                phoneInput.classList.remove('border-red-500', 'ring-2', 'ring-red-200');
                if (phoneError) phoneError.classList.add('hidden');
            }
        });

        phoneInput.addEventListener('keydown', (e) => {
            if (phoneInput.selectionStart <= 4 && (e.key === 'Backspace' || e.key === 'Delete')) {
                e.preventDefault();
            }
        });
        
        phoneInput.addEventListener('focus', () => {
            if (phoneInput.value.trim() === '' || phoneInput.value.trim() === '+998') {
                phoneInput.value = '+998 ';
            }
        });
    }

    // Splash Start Button
    const startBtn = document.getElementById('start-journey-btn');
    if (startBtn) {
        startBtn.addEventListener('click', (e) => {
            console.log("Start button clicked");
            startApp();
        });
    } else {
        console.error("Start button not found!");
    }

    const paxDisplay = document.getElementById('pax-count');
    const paxMinus = document.getElementById('pax-minus');
    const paxPlus = document.getElementById('pax-plus');
    
    if (paxMinus && paxDisplay) {
        paxMinus.addEventListener('click', () => {
            if (paxCount > 1) { paxCount--; paxDisplay.innerText = paxCount; updatePriceDisplay(); }
        });
    }
    
    if (paxPlus && paxDisplay) {
        paxPlus.addEventListener('click', () => {
            if (paxCount < 4) { paxCount++; paxDisplay.innerText = paxCount; updatePriceDisplay(); }
        });
    }

    const bagStatus = document.getElementById('baggage-status');
    if (bagStatus && bagStatus.parentElement && bagStatus.parentElement.parentElement) {
        bagStatus.parentElement.parentElement.addEventListener('click', () => {
            needsBaggage = !needsBaggage;
            bagStatus.innerText = needsBaggage ? t('common.yes') : t('common.no');
            bagStatus.style.color = needsBaggage ? "#2D2D2D" : "#888888";
            bagStatus.style.fontWeight = needsBaggage ? "bold" : "500";
            updatePriceDisplay();
        });
    }
    
    initAddressSearch();
}

function renderRegions(containerId, type) {
    const container = document.getElementById(containerId);
    container.innerHTML = '';
    regionsData.forEach(r => {
        const div = document.createElement('div');
        div.className = "p-4 bg-white rounded-2xl shadow-sm border border-gray-50 flex items-center justify-between cursor-pointer active:scale-95 transition-transform hover:bg-gray-50";
        div.innerHTML = `<span class="font-bold">${t('regions.' + r.id)}</span> <i class="fa-solid fa-chevron-right text-gray-300 text-sm"></i>`;
        div.onclick = () => selectRegion(type, r);
        container.appendChild(div);
    });
}

function selectRegion(type, region) {
    if (type === 'pickup') {
        selectedPickup.region = region;
        navigate('pickup-map-screen');
        setTimeout(() => initMapScreen('pickup', region), 300);
    } else {
        selectedDest.region = region;
        navigate('dest-map-screen');
        setTimeout(() => initMapScreen('dest', region), 300);
    }
}

async function initMapScreen(type, region) {
    const ready = await YandexTrackingEngine.ensureReady();
    if (!ready) return;

    // Reset search inputs on entering screen
    const searchInput = document.getElementById(`${type}-search-input`);
    if (searchInput) searchInput.value = '';
    const clearBtn = document.getElementById(`${type}-search-clear`);
    if (clearBtn) clearBtn.classList.add('hidden');
    const dropdown = document.getElementById(`${type}-search-suggestions`);
    if (dropdown) {
        dropdown.innerHTML = '';
        dropdown.classList.add('hidden');
    }

    // Wait for screen transition to stabilize
    requestAnimationFrame(() => {
        requestAnimationFrame(() => {
            if (type === 'pickup') {
                if (!pickupMap) {
                    pickupMap = new ymaps.Map('pickup-map-container', {
                        center: [Number(region.lat), Number(region.lng)],
                        zoom: 13,
                        controls: []
                    }, { 
                        suppressMapOpenBlock: true,
                        autoFitToViewport: 'always'
                    });
                    
                    pickupMap.events.add('click', (e) => {
                        const coords = e.get('coords');
                        updateMarkerAndAddress('pickup', { lat: coords[0], lng: coords[1] });
                    });
                } else {
                    pickupMap.setCenter([Number(region.lat), Number(region.lng)], 13);
                }
                
                // Automatically initialize marker and address at the center of the region on map load
                updateMarkerAndAddress('pickup', { lat: Number(region.lat), lng: Number(region.lng) });
                
                // Critical for rendering tiles
                pickupMap.container.fitToViewport();
                setTimeout(() => { 
                    if (pickupMap) {
                        pickupMap.container.fitToViewport();
                        pickupMap.setZoom(pickupMap.getZoom());
                    }
                }, 400);
                setTimeout(() => { if (pickupMap) pickupMap.container.fitToViewport(); }, 1200);
                
            } else {
                if (!destMap) {
                    destMap = new ymaps.Map('dest-map-container', {
                        center: [Number(region.lat), Number(region.lng)],
                        zoom: 13,
                        controls: []
                    }, { 
                        suppressMapOpenBlock: true,
                        autoFitToViewport: 'always'
                    });
                    
                    destMap.events.add('click', (e) => {
                        const coords = e.get('coords');
                        updateMarkerAndAddress('dest', { lat: coords[0], lng: coords[1] });
                    });
                } else {
                    destMap.setCenter([Number(region.lat), Number(region.lng)], 13);
                }
                
                // Automatically initialize marker and address at the center of the region on map load
                updateMarkerAndAddress('dest', { lat: Number(region.lat), lng: Number(region.lng) });
                
                // Critical for rendering tiles
                destMap.container.fitToViewport();
                setTimeout(() => { 
                    if (destMap) {
                        destMap.container.fitToViewport();
                        destMap.setZoom(destMap.getZoom());
                    }
                }, 400);
                setTimeout(() => { if (destMap) destMap.container.fitToViewport(); }, 1200);
            }
        });
    });
}

function updateMarkerAndAddress(type, latlng) {
    const lat = Number(latlng.lat);
    const lng = Number(latlng.lng);
    const coords = [lat, lng];
    
    // Clear old state before updating
    if (type === 'pickup') {
        selectedPickup.address = '';
    } else {
        selectedDest.address = '';
    }
    
    setMarker(type, { lat, lng }, t('common.addressLoading'));
    
    // OpenStreetMap Nominatim reverse geocoding
    async function performGeocoding() {
        try {
            const controller = new AbortController();
            const timeoutId = setTimeout(() => controller.abort(), 5000);
            
            const url = `https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat=${lat}&lon=${lng}`;
            const response = await fetch(url, {
                headers: {
                    'Accept-Language': currentLang
                },
                signal: controller.signal
            });
            
            clearTimeout(timeoutId);
            
            if (!response.ok) {
                throw new Error("Nominatim API error");
            }
            
            const data = await response.json();
            
            if (data && data.address) {
                if (type === 'pickup') {
                    selectedPickup.rawAddress = data.address;
                } else {
                    selectedDest.rawAddress = data.address;
                }
                const addressName = formatUzbekAddress(data.address) || data.display_name;
                
                console.log("Selected coords:", coords);
                console.log("Resolved address:", addressName);
                
                setMarker(type, { lat, lng }, addressName);
            } else {
                throw new Error("No address found in response");
            }
        } catch (err) {
            console.warn("Geocoding failed/timed out:", err);
            console.log("Selected coords:", coords);
            
            const fallbackAddress = t('common.addressNotFound');
            console.log("Resolved address:", fallbackAddress);
            setMarker(type, { lat, lng }, fallbackAddress);
        }
    }
    
    performGeocoding();
}

function setMarker(type, latlng, addressName) {
    const map = type === 'pickup' ? pickupMap : destMap;
    const isPickup = type === 'pickup';
    
    if (isPickup) {
        selectedPickup.latlng = latlng;
        selectedPickup.address = addressName;
        document.getElementById('pickup-address-text').innerText = addressName;
        
        if (pickupMarker) {
            pickupMarker.geometry.setCoordinates([latlng.lat, latlng.lng]);
        } else {
            pickupMarker = new ymaps.Placemark([latlng.lat, latlng.lng], {}, {
                preset: 'islands#yellowDotIcon',
                draggable: true // Marker move bo'lsa
            });
            pickupMarker.events.add('dragend', function (e) {
                const newCoords = pickupMarker.geometry.getCoordinates();
                updateMarkerAndAddress('pickup', { lat: newCoords[0], lng: newCoords[1] });
            });
            map.geoObjects.add(pickupMarker);
        }
    } else {
        selectedDest.latlng = latlng;
        selectedDest.address = addressName;
        document.getElementById('dest-address-text').innerText = addressName;
        
        if (destMarker) {
            destMarker.geometry.setCoordinates([latlng.lat, latlng.lng]);
        } else {
            destMarker = new ymaps.Placemark([latlng.lat, latlng.lng], {}, {
                preset: 'islands#blackDotIcon',
                draggable: true // Marker move bo'lsa
            });
            destMarker.events.add('dragend', function (e) {
                const newCoords = destMarker.geometry.getCoordinates();
                updateMarkerAndAddress('dest', { lat: newCoords[0], lng: newCoords[1] });
            });
            map.geoObjects.add(destMarker);
        }
    }
}

// --- OSRM Polyline Decoder ---
// Decodes Google's encoded polyline format used by OSRM
// Returns array of [lat, lng] coordinates for Yandex Maps
function decodeOSRMPolyline(encoded) {
    const points = [];
    let index = 0, lat = 0, lng = 0;

    while (index < encoded.length) {
        let b, shift = 0, result = 0;
        do {
            b = encoded.charCodeAt(index++) - 63;
            result |= (b & 0x1f) << shift;
            shift += 5;
        } while (b >= 0x20);
        const dlat = (result & 1) ? ~(result >> 1) : (result >> 1);
        lat += dlat;

        shift = 0;
        result = 0;
        do {
            b = encoded.charCodeAt(index++) - 63;
            result |= (b & 0x1f) << shift;
            shift += 5;
        } while (b >= 0x20);
        const dlng = (result & 1) ? ~(result >> 1) : (result >> 1);
        lng += dlng;

        points.push([lat / 1e5, lng / 1e5]);
    }
    return points;
}

// Haversine distance between two [lat,lng] points in METERS
function haversineMeters(coord1, coord2) {
    const R = 6371000; // Earth radius in meters
    const lat1 = coord1[0] * Math.PI / 180;
    const lat2 = coord2[0] * Math.PI / 180;
    const dLat = (coord2[0] - coord1[0]) * Math.PI / 180;
    const dLng = (coord2[1] - coord1[1]) * Math.PI / 180;
    const a = Math.sin(dLat/2) * Math.sin(dLat/2) +
        Math.cos(lat1) * Math.cos(lat2) *
        Math.sin(dLng/2) * Math.sin(dLng/2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
    return R * c;
}

// Update trip info display with remaining distance only (no time)
function updateTripInfo(distMeters) {
    const km = distMeters / 1000;

    const distEl = document.getElementById('trip-distance');

    if (distEl) {
        if (km >= 1) {
            distEl.innerText = km.toFixed(1) + " " + t('trip.kmLeft');
        } else {
            distEl.innerText = Math.round(km * 1000) + " " + t('trip.mLeft');
        }
    }
}

// Fallback: draw a simple curved line if routing API fails
function drawFallbackLine(map, lat1, lng1, lat2, lng2) {
    const midLat = (lat1 + lat2) / 2;
    const midLng = (lng1 + lng2) / 2;
    // Add slight curve offset
    const offset = Math.abs(lat1 - lat2) * 0.15;
    const curvePoints = [
        [lat1, lng1],
        [midLat + offset, midLng],
        [lat2, lng2]
    ];
    const line = new ymaps.Polyline(curvePoints, {}, {
        strokeColor: '#111111',
        strokeWidth: 4,
        strokeOpacity: 0.8,
        strokeStyle: 'shortdash'
    });
    map.geoObjects.add(line);

    // Estimate distance (straight line * 1.3 for road factor)
    const R = 6371;
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLon = (lng2 - lng1) * Math.PI / 180;
    const a = Math.sin(dLat/2) * Math.sin(dLat/2) +
        Math.cos(lat1 * Math.PI/180) * Math.cos(lat2 * Math.PI/180) *
        Math.sin(dLon/2) * Math.sin(dLon/2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
    const dist = Math.round(R * c * 1.3);
    const dur = Math.round(dist / 80 * 60); // ~80km/h avg

    routeState.ready = true;
    routeState.distance = dist * 1000;
    routeState.duration = dur * 60;
    routeState.distanceText = dist + " " + t('common.km');
    routeState.durationText = '';

    document.getElementById('preview-distance').innerText = routeState.distanceText;
    updateOrderButton();
    updatePriceDisplay();

    map.setBounds([[Math.min(lat1,lat2)-0.1, Math.min(lng1,lng2)-0.1], [Math.max(lat1,lat2)+0.1, Math.max(lng1,lng2)+0.1]], {
        checkZoomRange: true, zoomMargin: 60
    });
}

function startTaxiBookingFlow() {
    window._deliveryFlowMode = null;
    
    // Clear previous addresses
    selectedPickup = { region: null, latlng: null, address: '' };
    selectedDest = { region: null, latlng: null, address: '' };
    
    const fromEl = document.getElementById('from-display');
    if (fromEl) fromEl.innerText = t('taxi.fromPlaceholder');
    const toEl = document.getElementById('to-display');
    if (toEl) toEl.innerText = t('taxi.toPlaceholder');
    
    // Reset route UI
    routeState.ready = false;
    const previewSection = document.getElementById('route-preview-section');
    if (previewSection) {
        previewSection.classList.remove('opacity-100', 'translate-y-0', 'pointer-events-auto');
        previewSection.classList.add('opacity-0', 'translate-y-10', 'pointer-events-none');
        setTimeout(() => { previewSection.style.display = 'none'; }, 500);
    }
    const mapPreviewContainer = document.getElementById('taxi-map-preview-container');
    if (mapPreviewContainer) {
        mapPreviewContainer.classList.remove('opacity-100', 'translate-y-0', 'pointer-events-auto');
        mapPreviewContainer.classList.add('opacity-0', 'translate-y-10', 'pointer-events-none');
        setTimeout(() => { mapPreviewContainer.style.display = 'none'; }, 500);
    }
    const stickyBottomBar = document.getElementById('taxi-sticky-bottom-bar');
    if (stickyBottomBar) {
        stickyBottomBar.classList.remove('opacity-100', 'translate-y-0', 'pointer-events-auto');
        stickyBottomBar.classList.add('opacity-0', 'translate-y-10', 'pointer-events-none');
        setTimeout(() => { stickyBottomBar.style.display = 'none'; }, 500);
    }
    const emptyState = document.getElementById('home-empty-state');
    if (emptyState) {
        emptyState.style.display = 'flex';
        emptyState.classList.add('opacity-100', 'scale-100');
    }

    // Go to pickup region screen first
    navigate('pickup-region-screen');
}

function startDeliveryFlow() {
    window._deliveryFlowMode = 'pickup';
    
    // Clear previous addresses
    delPickup = { region: null, latlng: null, address: '' };
    delDest = { region: null, latlng: null, address: '' };
    
    const fromEl = document.getElementById('del-from-display');
    if (fromEl) fromEl.innerText = t('delivery.fromPlaceholder');
    const toEl = document.getElementById('del-to-display');
    if (toEl) toEl.innerText = t('delivery.toPlaceholder');
    
    // Reset route UI
    delRouteState.ready = false;
    const previewSection = document.getElementById('del-route-preview');
    if (previewSection) {
        previewSection.classList.remove('opacity-100', 'translate-y-0', 'pointer-events-auto');
        previewSection.classList.add('opacity-0', 'translate-y-10', 'pointer-events-none');
        setTimeout(() => { previewSection.style.display = 'none'; }, 500);
    }
    
    navigate('pickup-region-screen');
}

function startPickupFlow() {
    navigate('pickup-region-screen');
}

function startDestFlow() {
    if (!selectedPickup.latlng) {
        alert(t('taxi.selectPickupFirst'));
        // Optional: animate the pickup input to draw attention
        const pickupContainer = document.getElementById('from-display')?.parentElement;
        if (pickupContainer) {
            pickupContainer.classList.add('animate-pulse', 'bg-yellow-50');
            setTimeout(() => pickupContainer.classList.remove('animate-pulse', 'bg-yellow-50'), 1000);
        }
        return;
    }
    navigate('dest-region-screen');
}

function goBackFromPickup() {
    if (window._deliveryFlowMode === 'pickup') {
        navigate('delivery-screen');
    } else if (selectedPickup.latlng) {
        navigate('taxi-screen');
    } else {
        navigate('home-screen');
    }
}

function goBackFromDest() {
    if (window._deliveryFlowMode === 'dest') {
        navigate('delivery-screen');
    } else if (selectedDest.latlng) {
        navigate('taxi-screen');
    } else {
        navigate('taxi-screen');
    }
}

// --- Map Logic ---
function zoomMap(type, dir) {
    const map = type === 'pickup' ? pickupMap : destMap;
    if (!map) return;
    const currentZoom = map.getZoom();
    map.setZoom(currentZoom + dir, { checkZoomRange: true, duration: 200 });
}

function drawHomeRoute() {
    YandexTrackingEngine.buildHomeRoute();
}

function confirmPickupLocation() {
    // Check if in delivery flow mode
    if (window._deliveryFlowMode === 'pickup') {
        if (!selectedPickup.latlng || !selectedPickup.address || selectedPickup.address === t('common.addressLoading')) {
            alert(t('taxi.addressWaitOrMap'));
            return;
        }
        delPickup.latlng = { ...selectedPickup.latlng };
        delPickup.address = selectedPickup.address;
        delPickup.region = selectedPickup.region;
        const display = document.getElementById('del-from-display');
        if (display) display.innerText = delPickup.address;
        
        if (!delDest.latlng) {
            window._deliveryFlowMode = 'dest';
            navigate('dest-region-screen');
        } else {
            window._deliveryFlowMode = null;
            navigate('delivery-screen');
            if (delPickup.latlng && delDest.latlng) {
                setTimeout(() => buildDeliveryRoute(), 500);
            }
        }
        return;
    }

    if (!selectedPickup.latlng || !selectedPickup.address || selectedPickup.address === t('common.addressLoading')) {
        alert(t('taxi.addressWaitOrMap'));
        return;
    }
    const display = document.getElementById('from-display');
    if (display) display.innerText = selectedPickup.address;
    
    if (!selectedDest.latlng) {
        navigate('dest-region-screen');
    } else {
        // We already have a destination, just go back to taxi screen to recalculate
        routeState.ready = false;
        updatePriceDisplay();
        navigate('taxi-screen');
        setTimeout(() => updateOrderButton(), 100);
        setTimeout(() => {
            YandexTrackingEngine.showHomeMap();
            setTimeout(() => drawHomeRoute(), 500);
        }, 400);
    }
}

function confirmDestLocation() {
    // Check if in delivery flow mode
    if (window._deliveryFlowMode === 'dest') {
        if (!selectedDest.latlng || !selectedDest.address || selectedDest.address === t('common.addressLoading')) {
            alert(t('taxi.addressWaitOrMap'));
            return;
        }
        delDest.latlng = { ...selectedDest.latlng };
        delDest.address = selectedDest.address;
        delDest.region = selectedDest.region;
        const display = document.getElementById('del-to-display');
        if (display) display.innerText = delDest.address;
        window._deliveryFlowMode = null;
        navigate('delivery-screen');
        // Build route
        if (delPickup.latlng && delDest.latlng) {
            delRouteState.ready = false;
            updateDeliveryPriceDisplay();
            setTimeout(() => buildDeliveryRoute(), 500);
        }
        return;
    }

    if (!selectedDest.latlng || !selectedDest.address || selectedDest.address === t('common.addressLoading')) {
        alert(t('taxi.addressWaitOrMap'));
        return;
    }
    const display = document.getElementById('to-display');
    if (display) display.innerText = selectedDest.address;

    // Reset route state and update button
    routeState.ready = false;
    document.getElementById('preview-distance').innerText = '...';
    updatePriceDisplay();

    localStorage.setItem(DB_SKIP_ONBOARDING, 'true');
    navigate('taxi-screen');

    // Update button to loading state immediately
    setTimeout(() => updateOrderButton(), 100);

    // Build route after navigation settles
    setTimeout(() => {
        YandexTrackingEngine.showHomeMap();
        // Small extra delay for map container to get dimensions
        setTimeout(() => drawHomeRoute(), 500);
    }, 400);
}

function showLocationDeniedModal(reason) {
    // Detect if iOS (iPhone/iPad)
    const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream;
    
    const existingModal = document.getElementById('location-denied-modal');
    if (existingModal) existingModal.remove();
    
    const modal = document.createElement('div');
    modal.id = 'location-denied-modal';
    modal.style.cssText = `
        position: fixed;
        inset: 0;
        z-index: 9999;
        background: rgba(0,0,0,0.55);
        display: flex;
        align-items: flex-end;
        justify-content: center;
        padding: 0;
        animation: fadeIn 0.2s ease;
    `;
    
    let titleText = "Joylashuvga ruxsat yo'q";
    let subtitleText = "Manzilni avtomatik aniqlash uchun ruxsat bering";
    let instructionsHtml = "";

    if (isIOS) {
        if (reason === 'not_retrieved') {
            titleText = "Joylashuv aniqlanmadi";
            subtitleText = "Joylashuvga ruxsat berish uchun Safari sozlamalarini tekshiring";
            instructionsHtml = `
                <div style="margin-top:12px; background:#F5F5F7; border-radius:12px; padding:14px; text-align:left;">
                    <p style="font-size:12px; font-weight:700; color:#1C1C1E; margin:0 0 6px;">📱 Safari sozlamalari:</p>
                    <p style="font-size:12px; color:#3C3C43; margin:0 0 4px;">Joylashuvga ruxsat berish uchun Safari sozlamalarini tekshiring.</p>
                </div>
            `;
        } else {
            titleText = "Joylashuvga ruxsat berilmagan.";
            instructionsHtml = `
                <div style="margin-top:12px; background:#F5F5F7; border-radius:12px; padding:14px; text-align:left;">
                    <p style="font-size:13px; font-weight:700; color:#1C1C1E; margin:0 0 8px;">iPhone:</p>
                    <p style="font-size:12px; color:#3C3C43; margin:0 0 4px;"><b>Settings → Safari → Location → Allow</b></p>
                    <p style="font-size:11px; color:#8E8E93; margin:0 0 8px; text-align:left;">yoki</p>
                    <p style="font-size:12px; color:#3C3C43; margin:0;"><b>Settings → Privacy & Security → Location Services</b></p>
                </div>
            `;
        }
    } else {
        // Android or others
        instructionsHtml = `
            <div style="margin-top:12px; background:#F5F5F7; border-radius:12px; padding:14px; text-align:left;">
                <p style="font-size:12px; font-weight:700; color:#1C1C1E; margin:0 0 6px;">📱 Android uchun:</p>
                <p style="font-size:12px; color:#3C3C43; margin:0;"><b>Sozlamalar → Ilova → Brauzer → Ruxsatlar → Joylashuv</b></p>
            </div>
        `;
    }
    
    modal.innerHTML = `
        <div style="
            background: white;
            border-radius: 24px 24px 0 0;
            width: 100%;
            max-width: 480px;
            padding: 24px 20px 36px;
            animation: slideUp 0.3s cubic-bezier(0.32,0.72,0,1);
        ">
            <div style="width:40px;height:4px;background:#E5E5EA;border-radius:4px;margin:0 auto 20px;"></div>
            <div style="display:flex;align-items:center;gap:14px;margin-bottom:16px;">
                <div style="width:48px;height:48px;border-radius:14px;background:#FFF3E0;display:flex;align-items:center;justify-content:center;flex-shrink:0;">
                    <span style="font-size:24px;">📍</span>
                </div>
                <div>
                    <p style="font-size:16px;font-weight:700;color:#1C1C1E;margin:0 0 2px;">${titleText}</p>
                    <p style="font-size:12px;color:#8E8E93;margin:0;">${subtitleText}</p>
                </div>
            </div>
            ${instructionsHtml}
            <button onclick="document.getElementById('location-denied-modal').remove()" style="
                margin-top: 18px;
                width: 100%;
                background: #FFD400;
                color: #1C1C1E;
                font-size: 15px;
                font-weight: 700;
                padding: 14px;
                border-radius: 14px;
                border: none;
                cursor: pointer;
                touch-action: manipulation;
            ">Tushunarli</button>
        </div>
    `;
    
    // Close on backdrop tap
    modal.addEventListener('click', (e) => {
        if (e.target === modal) modal.remove();
    });
    
    document.body.appendChild(modal);
}

function getCurrentLocation() {
    const isPickupScreen = currentScreen === 'pickup-map-screen';
    const activeMap = isPickupScreen ? pickupMap : destMap;
    const type = isPickupScreen ? 'pickup' : 'dest';

    if (!activeMap) {
        console.warn("Map not loaded yet.");
        return;
    }

    if (!navigator.geolocation) {
        console.error("GPS not supported by this browser.");
        return;
    }

    const addressEl = document.getElementById(type + '-address-text');
    if (addressEl) addressEl.innerText = t('common.gpsLocating');

    // Success handler
    function onSuccess(position) {
        const lat = position.coords.latitude;
        const lng = position.coords.longitude;
        console.log("GPS location found:", lat, lng);
        activeMap.setCenter([lat, lng], 16, { duration: 500 });
        updateMarkerAndAddress(type, { lat, lng });
    }

    // Error handler
    function onError(err) {
        console.error("Geolocation error:", err.code, err.message);
        if (addressEl) addressEl.innerText = t('common.selectAddress');
        
        const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream;
        
        if (err.code === 1) { // PERMISSION_DENIED
            showLocationDeniedModal('denied');
        } else {
            // POSITION_UNAVAILABLE, TIMEOUT or other errors
            if (isIOS) {
                showLocationDeniedModal('not_retrieved');
            } else if (err.code === 2) {
                alert(t('gps.errorPositionUnavailable'));
            } else if (err.code === 3) {
                alert(t('gps.errorTimeout'));
            } else {
                alert(t('gps.errorGeneric'));
            }
        }
    }

    const options = {
        enableHighAccuracy: true,
        timeout: 15000,
        maximumAge: 0
    };

    // 1. Call getCurrentPosition immediately to prevent Safari from blocking the native prompt due to async delay.
    navigator.geolocation.getCurrentPosition(onSuccess, onError, options);

    // 2. Perform permissions check asynchronously for iPhone / Safari or other browsers
    if (navigator.permissions && navigator.permissions.query) {
        navigator.permissions.query({ name: 'geolocation' }).then(function(result) {
            console.log("Permission status:", result.state);
            if (result.state === 'denied' && !document.getElementById('location-denied-modal')) {
                showLocationDeniedModal('denied');
            }
        }).catch(function(err) {
            console.log("Permissions query fallback for iOS/Safari:", err);
        });
    }
}



function startPickupFlow() {
    navigate('pickup-region-screen');
}

function startDestFlow() {
    navigate('dest-region-screen');
}

// --- Map Logic ---
function zoomMap(type, dir) {
    const map = type === 'pickup' ? pickupMap : destMap;
    if (!map) return;
    if (dir > 0) map.zoomIn();
    else map.zoomOut();
}

function drawHomeRoute() {
    YandexTrackingEngine.buildHomeRoute();
}

// --- Order Button State ---
function updateOrderButton() {
    const btn = document.getElementById('order-btn');
    if (!btn) return;

    const hasAddresses = selectedPickup.latlng && selectedDest.latlng && selectedPickup.address && selectedDest.address;

    if (hasAddresses && routeState.ready) {
        btn.disabled = false;
        btn.classList.remove('opacity-50', 'cursor-not-allowed');
        btn.classList.add('shadow-lg', 'shadow-yellow-500/20');
        btn.innerText = t('taxi.orderBtn');
    } else if (hasAddresses && !routeState.ready) {
        btn.disabled = true;
        btn.classList.add('opacity-50', 'cursor-not-allowed');
        btn.classList.remove('shadow-lg', 'shadow-yellow-500/20');
        btn.innerText = t('taxi.loadingRoute');
    } else {
        btn.disabled = true;
        btn.classList.add('opacity-50', 'cursor-not-allowed');
        btn.classList.remove('shadow-lg', 'shadow-yellow-500/20');
        btn.innerText = t('taxi.orderBtn');
    }
}

// --- Order Flow ---
function createOrder() {
    console.log("createOrder triggered", {selectedPickup, selectedDest, routeState});

    if (!selectedPickup.latlng || !selectedDest.latlng || !selectedPickup.address || !selectedDest.address) {
        alert(t('taxi.selectAddressesFirst'));
        return;
    }

    if (!routeState.ready) {
        alert(t('taxi.waitRouteCalc'));
        return;
    }

    const fromLat = Number(selectedPickup.latlng.lat);
    const fromLng = Number(selectedPickup.latlng.lng);
    const toLat = Number(selectedDest.latlng.lat);
    const toLng = Number(selectedDest.latlng.lng);

    if (isNaN(fromLat) || isNaN(fromLng) || isNaN(toLat) || isNaN(toLng)) {
        alert(t('taxi.invalidCoords'));
        return;
    }

    // Create new order with route data
    const order = {
        id: Date.now(),
        from: { name: selectedPickup.address, lat: fromLat, lng: fromLng },
        to: { name: selectedDest.address, lat: toLat, lng: toLng },
        pax: paxCount,
        baggage: needsBaggage,
        distance: routeState.distanceText,
        duration: routeState.durationText,
        price: routeState.priceText,
        status: 'pending',
        date: new Date().toISOString()
    };

    // Save to local storage
    const activeOrders = JSON.parse(localStorage.getItem(DB_ACTIVE_ORDERS) || '[]');
    activeOrders.push(order);
    localStorage.setItem(DB_ACTIVE_ORDERS, JSON.stringify(activeOrders));

    // Go to waiting screen
    navigate('waiting-screen');

    // Simulate nearby cars in waiting screen
    showNearbyCarsInWaiting();

    // Simulate finding a driver after 3-5 seconds
    setTimeout(() => {
        simulateDriverAcceptance(order);
    }, 4000 + Math.random() * 2000);
}

function showNearbyCars() {
    if (!homeMap || !mapsInitialized) return;
    
    // Clear existing
    nearbyMarkers.forEach(m => homeMap.geoObjects.remove(m));
    nearbyMarkers = [];
    
    const center = homeMap.getCenter();
    for (let i = 0; i < 4; i++) {
        const offsetLat = (Math.random() - 0.5) * 0.01;
        const offsetLng = (Math.random() - 0.5) * 0.01;
        const pos = [center[0] + offsetLat, center[1] + offsetLng];
        
        const marker = new ymaps.Placemark(pos, {
            iconRotate: Math.random() * 360
        }, {
            iconLayout: 'custom#taxiLayout',
            iconOffset: [-22, -22],
            iconShape: { type: 'Circle', coordinates: [22, 22], radius: 22 },
            iconPane: 'overlaps'
        });
        
        homeMap.geoObjects.add(marker);
        nearbyMarkers.push(marker);
    }
}

function showNearbyCarsInWaiting() {
    // We can use homeMap as background or just show it on the map preview if it's visible
    // For now, let's just make sure cars are on the map when searching
}

let globalLastRotation = null;
function calculateRotation(p1, p2) {
    if (!p1 || !p2) return globalLastRotation || 0;
    const dy = p2[0] - p1[0];
    const dx = p2[1] - p1[1];
    
    if (Math.abs(dx) < 0.000001 && Math.abs(dy) < 0.000001) {
        return globalLastRotation || 0;
    }
    
    let angle = (Math.atan2(dx, dy) * 180) / Math.PI;
    
    if (globalLastRotation !== null) {
        let diff = (angle - globalLastRotation) % 360;
        if (diff > 180) diff -= 360;
        else if (diff < -180) diff += 360;
        angle = globalLastRotation + diff;
    }
    
    globalLastRotation = angle;
    return angle;
}

function cancelOrder() {
    // Stop any running trip animation
    if (YandexTrackingEngine._animFrame) cancelAnimationFrame(YandexTrackingEngine._animFrame);
    if (YandexTrackingEngine.interval) clearInterval(YandexTrackingEngine.interval);

    navigate('home-screen');
    // Remove last pending order (simplified)
    let activeOrders = JSON.parse(localStorage.getItem(DB_ACTIVE_ORDERS) || '[]');
    activeOrders.pop();
    localStorage.setItem(DB_ACTIVE_ORDERS, JSON.stringify(activeOrders));
}

function simulateDriverAcceptance(order) {
    if(currentScreen !== 'waiting-screen') return; // User cancelled
    
    const driver = mockDrivers[Math.floor(Math.random() * mockDrivers.length)];
    
    // Update order status
    let activeOrders = JSON.parse(localStorage.getItem(DB_ACTIVE_ORDERS) || '[]');
    const index = activeOrders.findIndex(o => o.id === order.id);
    if(index !== -1) {
        activeOrders[index].status = 'active';
        activeOrders[index].driver = driver;
        localStorage.setItem(DB_ACTIVE_ORDERS, JSON.stringify(activeOrders));
    }
    
    startTrip(activeOrders[index]);
}

function startTrip(order) {
    if (!order) return;
    activeOrderId = order.id;

    // 1. UI Setup
    document.getElementById('trip-status').innerText = t('trip.statusDriverEnRoute');
    document.getElementById('trip-distance').innerText = "... " + t('common.km');
    if (order.price) {
        document.getElementById('trip-price-value').innerText = order.price;
    }
    
    if (order.from && order.from.name) document.getElementById('trip-from').innerText = order.from.name;
    if (order.to && order.to.name) document.getElementById('trip-to').innerText = order.to.name;
    
    if (order.driver) {
        document.getElementById('trip-driver-name').innerText = order.driver.name;
        document.getElementById('trip-car-info').innerText = `${order.driver.car} • ${order.driver.plate}`;
        document.getElementById('trip-driver-rating').innerText = order.driver.rating || '5.0';
        const driverImg = document.getElementById('trip-driver-img');
        if (driverImg) driverImg.src = "rasmlar/avatar.png";
    }

    const now = new Date();
    const locale = currentLang === 'uz' ? 'uz-UZ' : (currentLang === 'ru' ? 'ru-RU' : 'en-US');
    const dateStr = now.toLocaleDateString(locale, {day: 'numeric', month: 'short'}) + ', ' + now.getHours().toString().padStart(2, '0') + ':' + now.getMinutes().toString().padStart(2, '0');
    document.getElementById('trip-date').innerText = dateStr;

    // 2. Navigation
    navigate('active-trip-screen');

    // 3. Delegation to Engine
    YandexTrackingEngine.startTripTracking(order);
}

function completeTrip(orderId) {
    let activeOrders = JSON.parse(localStorage.getItem(DB_ACTIVE_ORDERS) || '[]');
    let completedOrders = JSON.parse(localStorage.getItem(DB_COMPLETED_ORDERS) || '[]');
    
    const index = activeOrders.findIndex(o => o.id === orderId);
    if(index !== -1) {
        const order = activeOrders.splice(index, 1)[0];
        order.status = 'completed';
        completedOrders.push(order);
        
        localStorage.setItem(DB_ACTIVE_ORDERS, JSON.stringify(activeOrders));
        localStorage.setItem(DB_COMPLETED_ORDERS, JSON.stringify(completedOrders));
    }
}

// --- Specific Screens Data Loading ---
function renderSafarlarim() {
    const list = document.getElementById('safarlarim-list');
    list.innerHTML = '';
    
    const activeOrders = JSON.parse(localStorage.getItem(DB_ACTIVE_ORDERS) || '[]');
    const completedOrders = JSON.parse(localStorage.getItem(DB_COMPLETED_ORDERS) || '[]');
    
    const allOrders = [...activeOrders, ...completedOrders].sort((a,b) => b.id - a.id);
    
    if (allOrders.length === 0) {
        list.innerHTML = `<p class="text-center text-textSecondary mt-10">${t('trips.noTrips')}</p>`;
        return;
    }
    
    allOrders.forEach(order => {
        const isCompleted = order.status === 'completed';
        const dateObj = new Date(order.date);
        const locale = currentLang === 'uz' ? 'uz-UZ' : (currentLang === 'ru' ? 'ru-RU' : 'en-US');
        const dateStr = dateObj.toLocaleDateString(locale) + ' ' + dateObj.toLocaleTimeString(locale, {hour:'2-digit', minute:'2-digit'});
        
        const card = document.createElement('div');
        card.className = "bg-white rounded-[1.5rem] p-5 shadow-[0_2px_10px_rgba(0,0,0,0.03)] border border-gray-50";
        card.innerHTML = `
            <div class="flex justify-between items-center mb-4">
                <span class="text-xs font-semibold text-textSecondary">${dateStr}</span>
                <span class="text-[10px] font-bold px-2 py-1 rounded-md ${isCompleted ? 'bg-gray-100 text-gray-500' : 'bg-green-50 text-green-600'}">
                    ${isCompleted ? t('trips.completed') : t('trips.active')}
                </span>
            </div>
            <div class="flex items-center gap-3 mb-2">
                <div class="w-2 h-2 rounded-full bg-primary border border-white ring-1 ring-gray-200"></div>
                <span class="text-sm font-bold">${order.from.name}</span>
            </div>
            <div class="w-0.5 h-3 bg-gray-200 ml-[3px] mb-2"></div>
            <div class="flex items-center gap-3 mb-4">
                <div class="w-2 h-2 rounded-full bg-graphite border border-white ring-1 ring-gray-200"></div>
                <span class="text-sm font-bold">${order.to.name}</span>
            </div>
            ${order.driver ? `
            <div class="border-t border-gray-50 pt-3 flex items-center gap-3">
                <img src="rasmlar/avatar.png" class="w-8 h-8 rounded-full object-cover">
                <div>
                    <p class="text-xs font-bold">${order.driver.name}</p>
                    <p class="text-[10px] text-textSecondary">${order.driver.car}</p>
                </div>
            </div>
            ` : ''}
        `;
        
        if (!isCompleted && order.driver) {
            card.onclick = () => startTrip(order);
            card.classList.add('cursor-pointer', 'active:bg-gray-50', 'transition-colors');
        }
        
        list.appendChild(card);
    });
}

function renderXabarlar() {
    const list = document.getElementById('xabarlar-list');
    const chats = JSON.parse(localStorage.getItem(DB_CHATS) || '{}');
    
    // Ensure Support chat exists
    if (!chats['support']) {
        chats['support'] = {
            id: 'support',
            name: "Qo'llab-quvvatlash xizmati",
            icon: 'fa-headset',
            bg: 'bg-primary/10',
            textCol: 'text-primary',
            messages: [
                { id: Date.now(), sender: 'them', text: "Assalomu alaykum, sizga qanday yordam bera olaman?", timestamp: new Date().toISOString(), isWelcome: true }
            ]
        };
        localStorage.setItem(DB_CHATS, JSON.stringify(chats));
    }

    // Add Driver chat if active trip exists
    const activeOrders = JSON.parse(localStorage.getItem(DB_ACTIVE_ORDERS) || '[]');
    const currentTrip = activeOrders.find(o => o.status === 'active' && o.driver);
    if (currentTrip && !chats[`driver_${currentTrip.id}`]) {
        chats[`driver_${currentTrip.id}`] = {
            id: `driver_${currentTrip.id}`,
            name: currentTrip.driver.name,
            avatar: 'rasmlar/avatar.png',
            messages: [
                { id: Date.now(), sender: 'them', text: "Yo'ldaman, 5 daqiqada boraman.", timestamp: new Date().toISOString(), isDriverOnWay: true }
            ]
        };
        localStorage.setItem(DB_CHATS, JSON.stringify(chats));
    }

    // Ensure Promotions chat exists
    if (!chats['promo']) {
        chats['promo'] = {
            id: 'promo',
            name: "Aksiya va chegirmalar",
            icon: 'fa-tag',
            bg: 'bg-green-50',
            textCol: 'text-green-600',
            messages: [
                { id: Date.now(), sender: 'them', text: "Yangi yil chegirmalari boshlandi!", timestamp: new Date().toISOString(), isPromo: true }
            ]
        };
        localStorage.setItem(DB_CHATS, JSON.stringify(chats));
    }

    list.innerHTML = '';
    Object.values(chats).sort((a, b) => {
        const aLast = a.messages[a.messages.length - 1];
        const bLast = b.messages[b.messages.length - 1];
        return new Date(bLast.timestamp) - new Date(aLast.timestamp);
    }).forEach(chat => {
        const lastMsg = chat.messages[chat.messages.length - 1];
        const locale = currentLang === 'uz' ? 'uz-UZ' : (currentLang === 'ru' ? 'ru-RU' : 'en-US');
        const timeStr = new Date(lastMsg.timestamp).toLocaleTimeString(locale, { hour: '2-digit', minute: '2-digit' });
        
        let chatName = chat.name;
        if (chat.id === 'support') chatName = t('messages.supportTitle');
        else if (chat.id === 'promo') chatName = t('messages.promoTitle');

        let msgText = lastMsg.text;
        if (lastMsg.textKey) msgText = t(lastMsg.textKey);
        else if (lastMsg.isWelcome) msgText = t('messages.supportWelcome');
        else if (lastMsg.isPromo) msgText = t('messages.promoWelcome');
        else if (lastMsg.isDriverOnWay) msgText = t('messages.driverOnWay');
        else if (lastMsg.isDriverWelcome) msgText = t('chat.driverWelcome');
        
        const div = document.createElement('div');
        div.className = "p-4 border-b border-gray-50 flex items-center gap-4 bg-white active:bg-gray-50 cursor-pointer transition-colors relative";
        div.onclick = () => {
            chat.unread = false;
            localStorage.setItem(DB_CHATS, JSON.stringify(chats));
            showChatModal(chat.id);
        };
        div.innerHTML = `
            <div class="w-12 h-12 rounded-full ${chat.bg || 'bg-gray-100'} flex items-center justify-center ${chat.textCol || 'text-graphite'} relative overflow-hidden">
                ${chat.avatar ? `<img src="${chat.avatar}" class="w-full h-full object-cover">` : `<i class="fa-solid ${chat.icon} text-xl"></i>`}
                ${chat.unread ? `<div class="absolute top-0 right-0 w-3 h-3 bg-red-500 rounded-full border-2 border-white"></div>` : ''}
            </div>
            <div class="flex-1 overflow-hidden">
                <div class="flex justify-between items-center mb-1">
                    <h3 class="font-bold text-sm ${chat.unread ? 'text-graphite' : 'text-graphite'}">${chatName}</h3>
                    <span class="text-[10px] text-textSecondary">${timeStr}</span>
                </div>
                <p class="text-xs ${chat.unread ? 'text-graphite font-bold' : 'text-textSecondary'} truncate">${msgText}</p>
            </div>
        `;
        list.appendChild(div);
    });
}

function renderProfil() {
    const userStr = localStorage.getItem(DB_USER);
    if(userStr) {
        const user = JSON.parse(userStr);
        const name = user.name || t('profile.defaultName');
        const surname = user.surname || '';
        document.getElementById('profile-name').innerText = `${name} ${surname}`.trim();
        document.getElementById('profile-phone').innerText = user.phone || '+998';
        
        const avatarImg = user.gender === 'female' ? 'rasmlar/girlavatar.png' : 'rasmlar/boyavatar.png';
        const profileAvatar = document.getElementById('profile-avatar');
        if (profileAvatar) profileAvatar.src = avatarImg;

        // Update greeting on home screen if it exists
        const greeting = document.getElementById('home-greeting');
        if (greeting) {
            greeting.innerText = t('home.greetingUser', { name: name });
        }
    }
}

function openSettings() {
    const userStr = localStorage.getItem(DB_USER);
    if(userStr) {
        const user = JSON.parse(userStr);
        document.getElementById('settings-name').value = user.name || '';
        document.getElementById('settings-surname').value = user.surname || '';
        document.getElementById('settings-phone').innerText = user.phone || '+998';
        setSettingsGender(user.gender || 'male');
    }
    navigate('settings-screen');
}

function setSettingsGender(gender) {
    settingsGender = gender;
    document.querySelectorAll('.settings-gender-btn').forEach(btn => {
        btn.classList.remove('border-primary', 'bg-yellow-50');
        btn.classList.add('border-gray-50', 'bg-white');
    });
    const selected = document.getElementById(`settings-gender-${gender}`);
    if (selected) {
        selected.classList.remove('border-gray-50', 'bg-white');
        selected.classList.add('border-primary', 'bg-yellow-50');
    }
    
    // Live update settings avatar preview
    const settingsAvatar = document.getElementById('settings-avatar');
    if (settingsAvatar) {
        settingsAvatar.src = gender === 'female' ? 'rasmlar/girlavatar.png' : 'rasmlar/boyavatar.png';
    }
}

function saveSettings() {
    const name = document.getElementById('settings-name').value.trim();
    const surname = document.getElementById('settings-surname').value.trim();
    
    if (!name) {
        alert(t('login.nameError'));
        return;
    }
    
    const userStr = localStorage.getItem(DB_USER);
    if (userStr) {
        const user = JSON.parse(userStr);
        user.name = name;
        user.surname = surname;
        user.gender = settingsGender;
        localStorage.setItem(DB_USER, JSON.stringify(user));
        
        renderProfil();
        alert(t('settings.savedSuccess'));
        navigate('profil-screen');
    }
}

function openHelp() {
    // Direct to support chat
    showChatModal('support');
}

// --- Modals Logic ---
function showCallModal() {
    const modal = document.getElementById('call-modal');
    modal.classList.remove('hidden');
    setTimeout(() => {
        modal.classList.remove('opacity-0');
        modal.children[0].classList.remove('scale-95');
        modal.children[0].classList.add('scale-100');
    }, 10);
}

function hideCallModal() {
    const modal = document.getElementById('call-modal');
    modal.classList.add('opacity-0');
    modal.children[0].classList.remove('scale-100');
    modal.children[0].classList.add('scale-95');
    setTimeout(() => { modal.classList.add('hidden'); }, 300);
}

function showChatModal(chatId) {
    currentChatId = chatId;
    const modal = document.getElementById('chat-modal');
    const chats = JSON.parse(localStorage.getItem(DB_CHATS) || '{}');
    let chat = chats[chatId];
    
    if (!chat) {
        let driverName = "Haydovchi";
        if (chatId.startsWith('driver_')) {
            const orderId = chatId.split('_')[1];
            const activeOrders = JSON.parse(localStorage.getItem(DB_ACTIVE_ORDERS) || '[]');
            const order = activeOrders.find(o => o.id == orderId);
            if (order && order.driver) driverName = order.driver.name;
        }
        
        chat = {
            id: chatId,
            name: driverName,
            unread: false,
            messages: [
                { id: Date.now(), sender: 'them', text: "Assalomu alaykum! Men keldim, qayerdasiz?", timestamp: new Date().toISOString(), isDriverWelcome: true }
            ]
        };
        chats[chatId] = chat;
        localStorage.setItem(DB_CHATS, JSON.stringify(chats));
    }
    
    const chatTitle = chat.id === 'support' ? t('messages.supportTitle') : (chat.id === 'promo' ? t('messages.promoTitle') : chat.name);
    document.getElementById('chat-name').innerText = chatTitle;
    modal.classList.remove('hidden');
    setTimeout(() => {
        modal.classList.remove('translate-y-full');
    }, 10);
    
    renderMessages();
}

function renderMessages() {
    const container = document.getElementById('chat-messages');
    const chats = JSON.parse(localStorage.getItem(DB_CHATS) || '{}');
    const chat = chats[currentChatId];
    
    if (!chat) return;
    
    container.innerHTML = '';
    chat.messages.forEach(msg => {
        const isMe = msg.sender === 'me';
        const locale = currentLang === 'uz' ? 'uz-UZ' : (currentLang === 'ru' ? 'ru-RU' : 'en-US');
        const timeStr = new Date(msg.timestamp).toLocaleTimeString(locale, { hour: '2-digit', minute: '2-digit' });
        
        let msgText = msg.text;
        if (msg.textKey) msgText = t(msg.textKey);
        else if (msg.isWelcome) msgText = t('messages.supportWelcome');
        else if (msg.isPromo) msgText = t('messages.promoWelcome');
        else if (msg.isDriverOnWay) msgText = t('messages.driverOnWay');
        else if (msg.isDriverWelcome) msgText = t('chat.driverWelcome');

        const div = document.createElement('div');
        div.className = `flex ${isMe ? 'self-end' : 'self-start'} max-w-[85%] group relative mb-1`;
        div.innerHTML = `
            <div class="${isMe ? 'bg-primary' : 'bg-white'} p-3 rounded-2xl ${isMe ? 'rounded-tr-sm border border-yellow-400' : 'rounded-tl-sm border border-gray-50'} shadow-sm text-sm relative">
                <p class="whitespace-pre-wrap">${msgText}</p>
                <div class="flex items-center justify-end gap-1 mt-1">
                    <div class="text-[9px] ${isMe ? 'text-graphite opacity-70' : 'text-textSecondary'}">${timeStr} ${msg.edited ? t('chat.edited') : ''}</div>
                    ${isMe ? `<button onclick="toggleMessageMenu(${msg.id})" class="text-textSecondary/50 hover:text-graphite transition-colors ml-1"><i class="fa-solid fa-ellipsis-vertical text-[10px]"></i></button>` : ''}
                </div>
                ${isMe ? `
                <div id="msg-menu-${msg.id}" class="hidden absolute right-0 top-full mt-1 bg-white shadow-xl border border-gray-100 rounded-xl z-20 py-1 min-w-[100px]">
                    <button onclick="handleMessageAction('edit', ${msg.id})" class="w-full px-3 py-2 text-left text-xs text-graphite hover:bg-gray-50 flex items-center gap-2"><i class="fa-solid fa-pen text-[10px] text-textSecondary"></i> ${t('chat.edit')}</button>
                    <button onclick="handleMessageAction('delete', ${msg.id})" class="w-full px-3 py-2 text-left text-xs text-red-500 hover:bg-red-50 flex items-center gap-2"><i class="fa-solid fa-trash text-[10px]"></i> ${t('chat.delete')}</button>
                </div>
                ` : ''}
            </div>
        `;
        container.appendChild(div);
    });
    
    container.scrollTop = container.scrollHeight;
}

function toggleMessageMenu(msgId) {
    const menu = document.getElementById(`msg-menu-${msgId}`);
    const allMenus = document.querySelectorAll('[id^="msg-menu-"]');
    allMenus.forEach(m => { if(m !== menu) m.classList.add('hidden'); });
    menu.classList.toggle('hidden');
    
    // Auto-hide when clicking elsewhere
    const hideMenu = (e) => {
        if (!menu.contains(e.target) && !e.target.closest('button')) {
            menu.classList.add('hidden');
            document.removeEventListener('click', hideMenu);
        }
    };
    if(!menu.classList.contains('hidden')) {
        setTimeout(() => document.addEventListener('click', hideMenu), 10);
    }
}

function handleMessageAction(action, msgId) {
    const chats = JSON.parse(localStorage.getItem(DB_CHATS) || '{}');
    const chat = chats[currentChatId];
    const msgIndex = chat.messages.findIndex(m => m.id === msgId);
    
    if (msgIndex === -1) return;
    
    if (action === 'delete') {
        if (confirm(t('chat.confirmDelete'))) {
            chat.messages.splice(msgIndex, 1);
            localStorage.setItem(DB_CHATS, JSON.stringify(chats));
            renderMessages();
            renderXabarlar();
        }
    } else if (action === 'edit') {
        const newText = prompt(t('chat.editPrompt'), chat.messages[msgIndex].text);
        if (newText !== null && newText.trim() !== '') {
            chat.messages[msgIndex].text = newText.trim();
            chat.messages[msgIndex].edited = true;
            localStorage.setItem(DB_CHATS, JSON.stringify(chats));
            renderMessages();
            renderXabarlar();
        }
    }
}

function hideChatModal() {
    const modal = document.getElementById('chat-modal');
    modal.classList.add('translate-y-full');
    setTimeout(() => { 
        modal.classList.add('hidden');
        currentChatId = null;
    }, 300);
}

function sendChatMessage() {
    const input = document.getElementById('chat-input');
    const text = input.value.trim();
    if (!text || !currentChatId) return;
    
    const chats = JSON.parse(localStorage.getItem(DB_CHATS) || '{}');
    const chat = chats[currentChatId];
    
    const newMsg = {
        id: Date.now(),
        sender: 'me',
        text: text,
        timestamp: new Date().toISOString()
    };
    
    chat.messages.push(newMsg);
    localStorage.setItem(DB_CHATS, JSON.stringify(chats));
    
    input.value = '';
    renderMessages();
    renderXabarlar();
    
    // Auto replies
    if (currentChatId === 'support') {
        const replyKeys = [
            "chat.supportReply1",
            "chat.supportReply2",
            "chat.supportReply3"
        ];
        const randomKey = replyKeys[Math.floor(Math.random() * replyKeys.length)];
        setTimeout(() => addThemMessage(currentChatId, t(randomKey), randomKey), 1500);
    } else if (currentChatId.startsWith('driver_')) {
        const replyKeys = [
            "chat.driverReply1",
            "chat.driverReply2",
            "chat.driverReply3",
            "chat.driverReply4"
        ];
        const randomKey = replyKeys[Math.floor(Math.random() * replyKeys.length)];
        setTimeout(() => addThemMessage(currentChatId, t(randomKey), randomKey), 2000);
    }
}

function addThemMessage(chatId, text, textKey = null) {
    const chats = JSON.parse(localStorage.getItem(DB_CHATS) || '{}');
    const chat = chats[chatId];
    if (!chat) return;
    
    const newMsg = {
        id: Date.now(),
        sender: 'them',
        text: text,
        timestamp: new Date().toISOString()
    };
    if (textKey) newMsg.textKey = textKey;
    chat.messages.push(newMsg);
    
    if (currentChatId !== chatId) {
        chat.unread = true;
    }
    
    localStorage.setItem(DB_CHATS, JSON.stringify(chats));
    if (currentChatId === chatId) renderMessages();
    renderXabarlar();
}
function openActiveDriverChat() {
    if (activeOrderId) {
        showChatModal(`driver_${activeOrderId}`);
    }
}

// --- Address Search and Geocoding using OpenStreetMap Nominatim API ---

// Persistent Search Cache with safe localStorage saving
const DB_SEARCH_CACHE = 'vijdongo_search_cache';
let searchCache = {};
try {
    const cached = localStorage.getItem(DB_SEARCH_CACHE);
    if (cached) {
        searchCache = JSON.parse(cached);
    }
} catch (e) {
    console.error("Failed to load search cache from localStorage:", e);
}

function saveSearchCache(key, value) {
    searchCache[key] = value;
    try {
        const serialized = JSON.stringify(searchCache);
        if (serialized.length > 2 * 1024 * 1024) { // Prune if > 2MB
            const keys = Object.keys(searchCache);
            for (let i = 0; i < Math.min(20, keys.length); i++) {
                delete searchCache[keys[i]];
            }
        }
        localStorage.setItem(DB_SEARCH_CACHE, JSON.stringify(searchCache));
    } catch (e) {
        console.error("Failed to save search cache to localStorage:", e);
        try {
            localStorage.removeItem(DB_SEARCH_CACHE);
            searchCache = {};
            searchCache[key] = value;
            localStorage.setItem(DB_SEARCH_CACHE, JSON.stringify(searchCache));
        } catch (e2) {
            console.error("Failed to reset localStorage search cache:", e2);
        }
    }
}

// Global POI Index for fuzzy search & auto-complete
const poiIndex = [];

function indexPoiItem(item) {
    const name = item.display_name;
    if (!name) return;
    
    const lat = item.lat;
    const lon = item.lon;
    const coordKey = `${lat.toFixed(4)}_${lon.toFixed(4)}`;
    
    const exists = poiIndex.some(idxItem => {
        const idxLat = idxItem.lat;
        const idxLon = idxItem.lon;
        const idxKey = `${idxLat.toFixed(4)}_${idxLon.toFixed(4)}`;
        return idxKey === coordKey;
    });
    
    if (!exists) {
        poiIndex.push({
            name: name,
            cleanName: cleanName(name),
            item: item,
            lat: lat,
            lon: lon
        });
    }
}

function cleanName(str) {
    if (!str) return '';
    return str
        .toLowerCase()
        .replace(/[‘’`']/g, '')
        .replace(/o[‘’`']?g/g, 'og')
        .replace(/o[‘’`']/g, 'o')
        .replace(/g[‘’`']/g, 'g')
        .replace(/(shahri|viloyati|Respublikasi|tumani|tuman|shahar|viloyat|respublika)/gi, '')
        .trim();
}

function matchNames(name1, name2) {
    const c1 = cleanName(name1);
    const c2 = cleanName(name2);
    if (!c1 || !c2) return false;
    return c1.includes(c2) || c2.includes(c1);
}

function isResultInRegion(item, regionId) {
    if (!regionId) return false;
    const addr = item.address || {};
    const state = addr.state || '';
    const city = addr.city || addr.town || addr.village || '';
    const displayName = item.display_name || '';

    switch (regionId) {
        case 'toshkent_sh':
            return (
                matchNames(city, 'Toshkent') && 
                !matchNames(state, 'Toshkent viloyati')
            ) || (
                matchNames(state, 'Toshkent') && 
                !matchNames(state, 'Toshkent viloyati')
            );
        case 'toshkent_v':
            return matchNames(state, 'Toshkent viloyati') || matchNames(displayName, 'Toshkent viloyati');
        case 'qoraqalpogiston':
            return (
                matchNames(state, 'Qoraqalpog‘iston') || 
                matchNames(state, 'Karakalpakstan') || 
                matchNames(state, 'Каракалпакстан') || 
                matchNames(displayName, 'Qoraqalpog‘iston') ||
                matchNames(displayName, 'Karakalpakstan') ||
                matchNames(city, 'Nukus')
            );
        default:
            const regionObj = regionsData.find(r => r.id === regionId);
            if (!regionObj) return false;
            const cleanReg = cleanName(regionObj.name);
            return (
                cleanName(state).includes(cleanReg) || 
                cleanName(city).includes(cleanReg) || 
                cleanName(displayName).includes(cleanReg)
            );
    }
}

function getRegionSearchSuffix(region) {
    if (!region) return '';
    if (region.id === 'toshkent_sh') return 'Toshkent';
    if (region.id === 'toshkent_v') return 'Toshkent viloyati';
    if (region.id === 'qoraqalpogiston') return 'Nukus Qoraqalpogiston';
    return region.name;
}

function getDistanceKm(lat1, lon1, lat2, lon2) {
    const R = 6371; // Radius of the earth in km
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLon = (lon2 - lon1) * Math.PI / 180;
    const a = 
        Math.sin(dLat / 2) * Math.sin(dLat / 2) +
        Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) * 
        Math.sin(dLon / 2) * Math.sin(dLon / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c; // Distance in km
}

function mapPhotonPropertiesToAddress(props) {
    return {
        neighbourhood: props.suburb || props.locality,
        road: props.street,
        house_number: props.house_number,
        city_district: props.district,
        city: props.city,
        town: props.town,
        village: props.village,
        state: props.state
    };
}

function getResultType(item) {
    const isPhoton = !!item.properties;
    
    if (isPhoton) {
        const props = item.properties;
        const type = props.type;
        const osmKey = props.osm_key;
        const osmValue = props.osm_value;
        
        if (type === 'house' || osmKey === 'building' || props.postcode || props.house_number) {
            return 'uy';
        }
        if (type === 'street' || osmKey === 'highway') {
            return 'kocha';
        }
        if (type === 'locality' || osmValue === 'suburb' || osmValue === 'neighbourhood' || osmValue === 'residential') {
            return 'mahalla';
        }
        if (['school', 'kindergarten', 'college', 'university', 'hospital', 'clinic', 'pharmacy', 'bank', 'place_of_worship', 'restaurant', 'cafe'].includes(osmValue)) {
            return osmValue;
        }
        if (['school', 'kindergarten', 'college', 'university', 'hospital', 'clinic', 'pharmacy', 'bank', 'place_of_worship', 'restaurant', 'cafe'].includes(osmKey)) {
            return osmKey;
        }
        if (osmKey === 'shop' || osmKey === 'tourism' || osmKey === 'craft' || osmKey === 'industrial') {
            return 'shop';
        }
        if (type === 'district' || type === 'county') {
            return 'tuman';
        }
        if (type === 'city' || type === 'town' || type === 'village') {
            return 'shahar';
        }
        if (type === 'state') {
            return 'viloyat';
        }
    } else {
        const type = item.type;
        const cls = item.class;
        const addr = item.address || {};
        
        if (cls === 'building' || type === 'house' || addr.house_number) {
            return 'uy';
        }
        if (cls === 'highway' || addr.road) {
            return 'kocha';
        }
        if (type === 'suburb' || type === 'neighbourhood' || type === 'residential' || type === 'locality' || addr.neighbourhood || addr.suburb) {
            return 'mahalla';
        }
        if (['school', 'kindergarten', 'college', 'university', 'hospital', 'clinic', 'pharmacy', 'bank', 'place_of_worship', 'restaurant', 'cafe'].includes(type)) {
            return type;
        }
        if (['school', 'kindergarten', 'college', 'university', 'hospital', 'clinic', 'pharmacy', 'bank', 'place_of_worship', 'restaurant', 'cafe'].includes(cls)) {
            return cls;
        }
        if (cls === 'shop' || cls === 'tourism' || cls === 'craft' || cls === 'industrial') {
            return 'shop';
        }
        if (type === 'city_district' || type === 'district' || type === 'county' || addr.city_district || addr.county) {
            return 'tuman';
        }
        if (type === 'city' || type === 'town' || type === 'village' || addr.city || addr.town || addr.village) {
            return 'shahar';
        }
        if (type === 'state' || type === 'province' || addr.state) {
            return 'viloyat';
        }
    }
    return 'other';
}

function getTypeIcon(type) {
    const icons = {
        'school': '🏫',
        'kindergarten': '🏫',
        'college': '🏫',
        'university': '🏫',
        'hospital': '🏥',
        'clinic': '🏥',
        'pharmacy': '🏥',
        'bank': '🏦',
        'place_of_worship': '🕌',
        'shop': '🏪',
        'marketplace': '🏪',
        'restaurant': '🏪',
        'cafe': '🏪',
        'korxona': '🏪',
        
        'mahalla': '📍',
        'uy': '🏠',
        'kocha': '🛣',
        'tashkilot': '🏛',
        'tuman': '🗺',
        'shahar': '🏙',
        'viloyat': '🗺',
        'other': '📍'
    };
    return icons[type] || '📍';
}

function parsePoiQuery(query) {
    const q = query.toLowerCase();
    
    // Extract numbers
    const numberMatch = q.match(/\b\d+\b/);
    const queryNumber = numberMatch ? numberMatch[0] : null;
    
    let types = [];
    
    if (/\b(maktab|school|shkola|школа)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'school' });
    }
    if (/\b(bog['‘`a]?cha|kindergarten|sad|сад|детсад)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'kindergarten' });
    }
    if (/\b(kollej|college|колледж)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'college' });
    }
    if (/\b(universitet|university|institut|institute|вуз|ун-t)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'university' });
    }
    if (/\b(shifoxona|kasalxona|hospital|больниц)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'hospital' });
    }
    if (/\b(poliklinika|clinic|поликлиник|клиник)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'clinic' });
    }
    if (/\b(dorixona|pharmacy|apteka|аптек)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'pharmacy' });
    }
    if (/\b(bank|банк)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'bank' });
    }
    if (/\b(bozor|market|рынок|супермаркет)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'marketplace' });
        types.push({ key: 'shop', value: 'supermarket' });
    }
    if (/\b(dokon|do['‘`]kon|shop|magazin|магазин)\b/i.test(q)) {
        types.push({ key: 'shop', value: '*' });
    }
    if (/\b(restoran|restaurant|ресторан)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'restaurant' });
    }
    if (/\b(kafe|cafe|кафе)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'cafe' });
    }
    if (/\b(masjid|mosque|mechet|мечеть)\b/i.test(q)) {
        types.push({ key: 'amenity', value: 'place_of_worship' });
    }
    
    return { queryNumber, types };
}

function levenshteinDistance(a, b) {
    const matrix = [];
    for (let i = 0; i <= b.length; i++) matrix[i] = [i];
    for (let j = 0; j <= a.length; j++) matrix[0][j] = j;
    for (let i = 1; i <= b.length; i++) {
        for (let j = 1; j <= a.length; j++) {
            if (b.charAt(i - 1) === a.charAt(j - 1)) {
                matrix[i][j] = matrix[i - 1][j - 1];
            } else {
                matrix[i][j] = Math.min(
                    matrix[i - 1][j - 1] + 1, // substitution
                    matrix[i][j - 1] + 1,     // insertion
                    matrix[i - 1][j] + 1      // deletion
                );
            }
        }
    }
    return matrix[b.length][a.length];
}

function fuzzyMatch(query, target) {
    const q = cleanName(query);
    const t = cleanName(target);
    if (!q || !t) return 0;
    
    if (t.includes(q)) return 100;
    
    const qWords = q.split(/\s+/);
    const tWords = t.split(/\s+/);
    let matchedWords = 0;
    qWords.forEach(qw => {
        if (tWords.some(tw => tw.includes(qw) || qw.includes(tw))) {
            matchedWords++;
        }
    });
    
    if (matchedWords > 0) {
        return (matchedWords / qWords.length) * 80;
    }
    
    if (qWords.length === 1 && tWords.length === 1) {
        const dist = levenshteinDistance(q, t);
        const maxLen = Math.max(q.length, t.length);
        if (dist <= 2 && maxLen > 3) {
            return (1 - dist / maxLen) * 50;
        }
    }
    
    return 0;
}

function isPoiQueryMatch(item, queryNumber, types) {
    const name = item.display_name || '';
    const itemType = getResultType(item);
    
    const typeMatch = types.length === 0 || types.some(t => {
        if (t.value === 'school') {
            return ['school', 'college', 'university'].includes(itemType);
        }
        if (t.value === 'hospital') {
            return ['hospital', 'clinic'].includes(itemType);
        }
        return itemType === t.value || itemType === t.key;
    });
    
    if (!typeMatch) return false;
    
    if (queryNumber) {
        const regex = new RegExp(`\\b${queryNumber}\\b|${queryNumber}-|№${queryNumber}`);
        return regex.test(name);
    }
    
    return true;
}

function mapOverpassToStandard(element, activeRegion) {
    const tags = element.tags || {};
    const lat = element.lat || (element.center ? element.center.lat : null);
    const lon = element.lon || (element.center ? element.center.lon : null);
    
    if (!lat || !lon) return null;
    
    const address = {
        amenity: tags.name,
        road: tags["addr:street"] || tags.street,
        neighbourhood: tags["addr:neighbourhood"] || tags.neighbourhood || tags.suburb,
        city: tags["addr:city"] || tags.city || tags.town || tags.village,
        state: tags["addr:state"] || tags.state || tags.province,
        country: "O'zbekiston"
    };
    
    if (activeRegion) {
        if (!address.state) {
            address.state = activeRegion.name;
        }
        if (!address.city && activeRegion.id === 'toshkent_sh') {
            address.city = 'Toshkent';
        }
    }
    
    const type = tags.amenity || tags.shop || 'poi';
    const cls = tags.amenity ? 'amenity' : (tags.shop ? 'shop' : 'poi');
    
    let displayName = tags.name || '';
    if (address.road) displayName += `, ${address.road}`;
    if (address.city) displayName += `, ${address.city}`;
    if (address.state) displayName += `, ${address.state}`;
    
    return {
        lat: Number(lat),
        lon: Number(lon),
        display_name: displayName,
        address: address,
        type: type,
        class: cls
    };
}

async function fetchOverpass(query, activeRegion) {
    const trimmedQuery = query.trim();
    const { queryNumber, types } = parsePoiQuery(trimmedQuery);
    
    if (!activeRegion && types.length === 0) {
        return [];
    }
    
    let overpassQuery = '[out:json][timeout:15];\n(\n';
    const locationFilter = activeRegion 
        ? `(around:100000,${activeRegion.lat},${activeRegion.lng})`
        : `(37.0,56.0,46.0,74.0)`;
        
    if (types.length > 0) {
        types.forEach(t => {
            const keyVal = t.value === '*' ? `[~"^${t.key}$"~"."]` : `[${t.key}=${t.value}]`;
            
            if (queryNumber) {
                const numRegex = `\\b${queryNumber}\\b|${queryNumber}-|№${queryNumber}|${queryNumber}-son`;
                overpassQuery += `  node${keyVal}["name"~"${numRegex}",i]${locationFilter};\n`;
                overpassQuery += `  way${keyVal}["name"~"${numRegex}",i]${locationFilter};\n`;
                overpassQuery += `  relation${keyVal}["name"~"${numRegex}",i]${locationFilter};\n`;
            } else {
                const nameRegex = trimmedQuery.split(/\s+/).join('|');
                overpassQuery += `  node${keyVal}["name"~"${nameRegex}",i]${locationFilter};\n`;
                overpassQuery += `  way${keyVal}["name"~"${nameRegex}",i]${locationFilter};\n`;
                overpassQuery += `  relation${keyVal}["name"~"${nameRegex}",i]${locationFilter};\n`;
            }
        });
    } else {
        const nameRegex = trimmedQuery.split(/\s+/).join('|');
        overpassQuery += `  node["name"~"${nameRegex}",i]${locationFilter};\n`;
        overpassQuery += `  way["name"~"${nameRegex}",i]${locationFilter};\n`;
        overpassQuery += `  relation["name"~"${nameRegex}",i]${locationFilter};\n`;
    }
    
    overpassQuery += ');\nout body center;';
    
    try {
        const response = await fetch('https://overpass-api.de/api/interpreter', {
            method: 'POST',
            body: 'data=' + encodeURIComponent(overpassQuery),
            headers: {
                'Content-Type': 'application/x-www-form-urlencoded'
            }
        });
        
        if (!response.ok) {
            throw new Error("Overpass API response error");
        }
        
        const data = await response.json();
        if (data && data.elements) {
            const mapped = data.elements
                .map(el => mapOverpassToStandard(el, activeRegion))
                .filter(item => item !== null);
            return mapped;
        }
    } catch (e) {
        console.warn("Overpass query failed:", e);
    }
    return [];
}

function calculateRelevanceScore(item, query, activeRegion, rawAddr) {
    let score = 0;
    
    const isPhoton = !!item.properties;
    const displayName = isPhoton ? (item.properties.name + ", " + (item.properties.street || '')) : item.display_name;
    const type = getResultType(item);
    
    const weights = {
        'school': 5,
        'kindergarten': 5,
        'college': 5,
        'university': 5,
        'hospital': 5,
        'clinic': 5,
        'pharmacy': 5,
        'bank': 5,
        'place_of_worship': 5,
        'shop': 4,
        'marketplace': 4,
        'restaurant': 4,
        'cafe': 4,
        'mahalla': 3,
        'kocha': 2,
        'uy': 1,
        'other': 0
    };
    score += weights[type] || 0;
    
    if (activeRegion) {
        const inRegion = isResultInRegion(item, activeRegion.id);
        if (inRegion) {
            score += 1000;
            
            let selectedCity = '';
            if (rawAddr) {
                selectedCity = rawAddr.city || rawAddr.town || rawAddr.village || rawAddr.hamlet || '';
            }
            if (!selectedCity) {
                if (activeRegion.id === 'toshkent_sh') selectedCity = 'Toshkent';
                else if (activeRegion.id === 'qoraqalpogiston') selectedCity = 'Nukus';
                else if (activeRegion.id !== 'toshkent_v') {
                    selectedCity = activeRegion.name;
                }
            }
            const itemCity = item.address ? (item.address.city || item.address.town || item.address.village || item.address.hamlet || '') : '';
            if (selectedCity && itemCity && matchNames(itemCity, selectedCity)) {
                score += 3000;
            }
            
            let selectedDistrict = '';
            if (rawAddr) {
                selectedDistrict = rawAddr.city_district || rawAddr.district || rawAddr.county || '';
            }
            const itemDistrict = item.address ? (item.address.city_district || item.address.district || item.address.county || '') : '';
            if (selectedDistrict && itemDistrict && matchNames(itemDistrict, selectedDistrict)) {
                score += 2000;
            }
        }
    }
    
    const { queryNumber, types } = parsePoiQuery(query);
    if (isPoiQueryMatch(item, queryNumber, types)) {
        score += 5000; // POI matching boost
    }
    
    if (displayName.toLowerCase().includes(query.toLowerCase())) {
        score += 100;
    }
    
    return score;
}

function formatUzbekAddress(address) {
    if (!address) return '';
    const parts = [];
    
    // 0. Obyekt nomi
    let obyekt = address.amenity || address.school || address.hospital || address.clinic || address.pharmacy || address.bank || address.marketplace || address.shop || address.restaurant || address.cafe || address.place_of_worship || address.tourism || address.leisure || address.office || address.building;
    if (obyekt) {
        parts.push(obyekt.trim());
    }
    
    // 1. Ko'cha
    let street = address.road || address.street;
    if (street) {
        street = street.trim();
        if (!/ko'cha/i.test(street) && !/ko`cha/i.test(street) && !/ko‘cha/i.test(street) && !/улица/i.test(street) && !/ул\./i.test(street)) {
            street += " ko'chasi";
        }
        parts.push(street);
    }
    
    // 2. Mahalla
    let mahalla = address.neighbourhood || address.suburb || address.residential || address.island;
    if (mahalla) {
        mahalla = mahalla.trim();
        if (!/mahalla/i.test(mahalla) && !/маҳалла/i.test(mahalla)) {
            mahalla += " mahallasi";
        }
        parts.push(mahalla);
    }
    
    // 3. Uy raqami
    let houseNumber = address.house_number;
    if (houseNumber) {
        houseNumber = houseNumber.trim();
        if (/^\d+[A-Za-zА-Яа-я]?$/.test(houseNumber)) {
            houseNumber += "-uy";
        }
        parts.push(houseNumber);
    }
    
    // 4. Tuman (district)
    let tuman = address.city_district || address.district || address.county;
    if (tuman) {
        tuman = tuman.trim();
        const cityOrTown = address.city || address.town;
        if (cityOrTown && tuman.toLowerCase() === cityOrTown.toLowerCase()) {
            tuman = '';
        }
        const state = address.state || address.region;
        if (state && tuman.toLowerCase() === state.toLowerCase()) {
            tuman = '';
        }
        
        if (tuman) {
            if (!/tuman/i.test(tuman) && !/туман/i.test(tuman) && !/district/i.test(tuman)) {
                tuman += " tumani";
            }
            parts.push(tuman);
        }
    }
    
    // 5. Shahar (city, town, village)
    let shahar = address.city || address.town || address.village || address.hamlet;
    if (shahar) {
        shahar = shahar.trim();
        if (!/shahar/i.test(shahar) && !/город/i.test(shahar) && !/city/i.test(shahar)) {
            shahar += " shahri";
        }
        parts.push(shahar);
    }

    // 6. Viloyat (state/province)
    let viloyat = address.state || address.region;
    if (viloyat) {
        viloyat = viloyat.trim();
        viloyat = viloyat.replace(/(Region|Province|область|город)/gi, '').trim();
        viloyat = viloyat.replace(/o‘/gi, "o'").replace(/o‘/gi, "o'").replace(/g‘/gi, "g'");
        if (viloyat.toLowerCase() === 'toshkent') {
            const city = address.city || address.town || address.village;
            if (city && /toshkent/i.test(city)) {
                viloyat = "Toshkent shahri";
            } else {
                viloyat = "Toshkent viloyati";
            }
        } else if (viloyat.toLowerCase().includes('qoraqalpogiston') || viloyat.toLowerCase().includes('karakalpakstan') || viloyat.toLowerCase().includes('respublika')) {
            viloyat = "Qoraqalpog‘iston Respublikasi";
        } else {
            if (!/viloyat/i.test(viloyat) && !/shahar/i.test(viloyat)) {
                viloyat += " viloyati";
            }
        }
        parts.push(viloyat);
    }
    
    return parts.length > 0 ? parts.join(', ') : null;
}

function getSuggestionDisplay(item) {
    let title = '';
    let subtitle = '';
    
    const formatted = formatUzbekAddress(item.address);
    
    if (formatted) {
        const parts = formatted.split(', ');
        title = parts[0];
        if (parts.length > 1) {
            subtitle = parts.slice(1).join(', ');
        }
    } else {
        const parts = item.display_name.split(', ');
        title = parts[0] || '';
        if (parts.length > 1) {
            subtitle = parts.slice(1).join(', ');
        }
    }
    
    subtitle = subtitle.replace(/,?\s*(O'zbekiston|O‘zbekiston|Узбекистан|Uzbekistan)/i, '');
    subtitle = subtitle.replace(/,?\s*\b\d{6}\b/g, '');
    
    return { title, subtitle };
}

function selectSearchSuggestion(type, lat, lng, addressName) {
    const map = type === 'pickup' ? pickupMap : destMap;
    if (!map) return;
    
    map.setCenter([lat, lng], 16);
    
    setMarker(type, { lat, lng }, addressName);
    
    const dropdown = document.getElementById(`${type}-search-suggestions`);
    if (dropdown) dropdown.classList.add('hidden');
    
    const searchInput = document.getElementById(`${type}-search-input`);
    if (searchInput) {
        searchInput.value = addressName;
        const clearBtn = document.getElementById(`${type}-search-clear`);
        if (clearBtn) clearBtn.classList.remove('hidden');
    }
}

function vgDebounce(func, wait) {
    let timeout;
    return function executedFunction(...args) {
        const later = () => {
            clearTimeout(timeout);
            func(...args);
        };
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
    };
}

function renderResults(results, query, suggestionsDiv, type) {
    suggestionsDiv.innerHTML = '';
    
    if (results && results.length > 0) {
        results.forEach(item => {
            const { title, subtitle } = getSuggestionDisplay(item);
            const fullAddress = formatUzbekAddress(item.address) || item.display_name;
            const itemType = getResultType(item);
            const icon = getTypeIcon(itemType);
            
            const div = document.createElement('div');
            div.className = 'search-suggestion-item';
            div.innerHTML = `
                <div class="search-suggestion-icon" style="font-size: 16px;">
                    ${icon}
                </div>
                <div class="search-suggestion-text" style="flex: 1; min-width: 0;">
                    <span class="search-suggestion-title" style="display: block; font-weight: 700; font-size: 13.5px; color: #2D2D2D; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${title}</span>
                    <span class="search-suggestion-subtitle" style="display: block; font-size: 11px; color: #8E8E93; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; margin-top: 1px;">${subtitle || ''}</span>
                </div>
            `;
            
            div.addEventListener('click', () => {
                selectSearchSuggestion(type, Number(item.lat), Number(item.lon), fullAddress);
            });
            
            suggestionsDiv.appendChild(div);
        });
        suggestionsDiv.classList.remove('hidden');
    } else {
        const div = document.createElement('div');
        div.className = 'p-4 text-center text-xs text-textSecondary font-medium';
        div.innerText = currentLang === 'uz' ? 'Manzil topilmadi' : (currentLang === 'ru' ? 'Адрес не найден' : 'Address not found');
        suggestionsDiv.appendChild(div);
        suggestionsDiv.classList.remove('hidden');
    }
}

function initAddressSearch() {
    const types = ['pickup', 'dest'];
    
    types.forEach(type => {
        const input = document.getElementById(`${type}-search-input`);
        const suggestionsDiv = document.getElementById(`${type}-search-suggestions`);
        const clearBtn = document.getElementById(`${type}-search-clear`);
        
        if (!input || !suggestionsDiv) return;
        
        if (clearBtn) {
            clearBtn.addEventListener('click', () => {
                input.value = '';
                clearBtn.classList.add('hidden');
                suggestionsDiv.innerHTML = '';
                suggestionsDiv.classList.add('hidden');
                input.focus();
            });
        }
        
        const performSearch = vgDebounce(async (query) => {
            const trimmedQuery = query.trim();
            if (!trimmedQuery) {
                suggestionsDiv.innerHTML = '';
                suggestionsDiv.classList.add('hidden');
                return;
            }
            
            const activeRegion = type === 'pickup' ? selectedPickup.region : selectedDest.region;
            const rawAddr = type === 'pickup' ? selectedPickup.rawAddress : selectedDest.rawAddress;
            
            const cacheKey = `${trimmedQuery}_${activeRegion ? activeRegion.id : 'no_region'}_${currentLang}`;
            if (searchCache[cacheKey]) {
                console.log("DEBUG: Search cache hit for:", cacheKey);
                renderResults(searchCache[cacheKey], trimmedQuery, suggestionsDiv, type);
                return;
            }
            
            try {
                const lang = currentLang === 'uz' ? 'uz' : (currentLang === 'ru' ? 'ru' : 'en');
                
                let nominatimResultsRegion = [];
                let nominatimResultsGeneral = [];
                let photonResultsRegion = [];
                let photonResultsGeneral = [];
                let overpassResults = [];
                
                const regionSuffix = activeRegion ? getRegionSearchSuffix(activeRegion) : '';
                const nominatimPromises = [];
                
                // Stage 1: Nominatim Search
                if (activeRegion) {
                    const regionalUrl = `https://nominatim.openstreetmap.org/search?q=${encodeURIComponent(trimmedQuery + ', ' + regionSuffix)}&format=json&addressdetails=1&limit=15&countrycodes=uz&accept-language=${lang}`;
                    nominatimPromises.push(
                        fetch(regionalUrl, { headers: { 'User-Agent': 'VijdonGO-Taxi-App' } })
                            .then(res => res.ok ? res.json() : [])
                            .catch(err => { console.warn("Regional Nominatim fetch failed:", err); return []; })
                    );
                }
                
                const generalUrl = `https://nominatim.openstreetmap.org/search?q=${encodeURIComponent(trimmedQuery)}&format=json&addressdetails=1&limit=15&countrycodes=uz&accept-language=${lang}`;
                nominatimPromises.push(
                    fetch(generalUrl, { headers: { 'User-Agent': 'VijdonGO-Taxi-App' } })
                        .then(res => res.ok ? res.json() : [])
                        .catch(err => { console.warn("General Nominatim fetch failed:", err); return []; })
                );
                
                const nominatimRes = await Promise.all(nominatimPromises);
                if (activeRegion) {
                    nominatimResultsRegion = nominatimRes[0] || [];
                    nominatimResultsGeneral = nominatimRes[1] || [];
                } else {
                    nominatimResultsGeneral = nominatimRes[0] || [];
                }
                
                let nominatimCount = nominatimResultsRegion.length + nominatimResultsGeneral.length;
                const { queryNumber, types: parsedTypes } = parsePoiQuery(trimmedQuery);
                
                // Stage 2: Overpass API Fallback
                if (nominatimCount < 5 || parsedTypes.length > 0) {
                    overpassResults = await fetchOverpass(trimmedQuery, activeRegion);
                }
                
                // Photon Fallback (if Nominatim and Overpass combined are still sparse)
                if (nominatimCount + overpassResults.length < 3) {
                    const photonPromises = [];
                    if (activeRegion) {
                        let regionalPhotonUrl = `https://photon.komoot.io/api/?q=${encodeURIComponent(trimmedQuery + ', ' + regionSuffix)}&limit=15&countrycode=uz&lang=${lang}`;
                        if (activeRegion.lat && activeRegion.lng) {
                            regionalPhotonUrl += `&lat=${activeRegion.lat}&lon=${activeRegion.lng}`;
                        }
                        photonPromises.push(
                            fetch(regionalPhotonUrl)
                                .then(res => res.ok ? res.json() : null)
                                .then(data => data && data.features ? data.features : [])
                                .catch(err => { console.warn("Regional Photon fetch failed:", err); return []; })
                        );
                    }
                    
                    let generalPhotonUrl = `https://photon.komoot.io/api/?q=${encodeURIComponent(trimmedQuery)}&limit=15&countrycode=uz&lang=${lang}`;
                    if (activeRegion && activeRegion.lat && activeRegion.lng) {
                        generalPhotonUrl += `&lat=${activeRegion.lat}&lon=${activeRegion.lng}`;
                    }
                    photonPromises.push(
                        fetch(generalPhotonUrl)
                            .then(res => res.ok ? res.json() : null)
                            .then(data => data && data.features ? data.features : [])
                            .catch(err => { console.warn("General Photon fetch failed:", err); return []; })
                    );
                    
                    const photonRes = await Promise.all(photonPromises);
                    if (activeRegion) {
                        photonResultsRegion = photonRes[0] || [];
                        photonResultsGeneral = photonRes[1] || [];
                    } else {
                        photonResultsGeneral = photonRes[0] || [];
                    }
                }
                
                const combined = [];
                const seenCoords = new Set();
                
                function addUniqueItem(lat, lon, itemData) {
                    if (isNaN(lat) || isNaN(lon)) return;
                    const coordKey = `${lat.toFixed(4)}_${lon.toFixed(4)}`;
                    if (seenCoords.has(coordKey)) return;
                    seenCoords.add(coordKey);
                    combined.push(itemData);
                }
                
                // Add local matches first if fuzzy match score is high
                if (poiIndex.length > 0) {
                    poiIndex.forEach(entry => {
                        const matchScore = fuzzyMatch(trimmedQuery, entry.name);
                        if (matchScore > 30) {
                            const itemCopy = { ...entry.item };
                            itemCopy._localMatchScore = matchScore;
                            addUniqueItem(itemCopy.lat, itemCopy.lon, itemCopy);
                        }
                    });
                }
                
                nominatimResultsRegion.forEach(item => {
                    const lat = parseFloat(item.lat);
                    const lon = parseFloat(item.lon);
                    addUniqueItem(lat, lon, {
                        lat,
                        lon,
                        display_name: item.display_name,
                        address: item.address,
                        type: item.type,
                        class: item.class
                    });
                });
                
                nominatimResultsGeneral.forEach(item => {
                    const lat = parseFloat(item.lat);
                    const lon = parseFloat(item.lon);
                    addUniqueItem(lat, lon, {
                        lat,
                        lon,
                        display_name: item.display_name,
                        address: item.address,
                        type: item.type,
                        class: item.class
                    });
                });
                
                overpassResults.forEach(item => {
                    addUniqueItem(item.lat, item.lon, item);
                });
                
                photonResultsRegion.forEach(feature => {
                    const coords = feature.geometry.coordinates;
                    const lat = coords[1];
                    const lon = coords[0];
                    const props = feature.properties;
                    let displayName = props.name || '';
                    if (props.street) displayName += ', ' + props.street;
                    if (props.city) displayName += ', ' + props.city;
                    
                    addUniqueItem(lat, lon, {
                        lat,
                        lon,
                        display_name: displayName,
                        address: mapPhotonPropertiesToAddress(props),
                        properties: props
                    });
                });
                
                photonResultsGeneral.forEach(feature => {
                    const coords = feature.geometry.coordinates;
                    const lat = coords[1];
                    const lon = coords[0];
                    const props = feature.properties;
                    let displayName = props.name || '';
                    if (props.street) displayName += ', ' + props.street;
                    if (props.city) displayName += ', ' + props.city;
                    
                    addUniqueItem(lat, lon, {
                        lat,
                        lon,
                        display_name: displayName,
                        address: mapPhotonPropertiesToAddress(props),
                        properties: props
                    });
                });
                
                // Index all found results for future fuzzy lookup
                combined.forEach(item => {
                    indexPoiItem(item);
                });
                
                // Score
                combined.forEach(item => {
                    item._score = calculateRelevanceScore(item, trimmedQuery, activeRegion, rawAddr);
                    if (item._localMatchScore) {
                        item._score += item._localMatchScore;
                    }
                });
                
                // Sort
                const typeRank = {
                    'school': 15,
                    'kindergarten': 14,
                    'college': 13,
                    'university': 12,
                    'hospital': 11,
                    'clinic': 10,
                    'pharmacy': 9,
                    'bank': 8,
                    'place_of_worship': 7,
                    'shop': 6,
                    'marketplace': 6,
                    'restaurant': 6,
                    'cafe': 6,
                    'mahalla': 5,
                    'kocha': 4,
                    'uy': 3,
                    'tashkilot': 2,
                    'tuman': 1,
                    'shahar': 1,
                    'viloyat': 1
                };
                
                combined.sort((a, b) => {
                    if (b._score !== a._score) {
                        return b._score - a._score;
                    }
                    const rankA = typeRank[getResultType(a)] || 0;
                    const rankB = typeRank[getResultType(b)] || 0;
                    return rankB - rankA;
                });
                
                const finalResults = combined.slice(0, 10);
                saveSearchCache(cacheKey, finalResults);
                
                renderResults(finalResults, trimmedQuery, suggestionsDiv, type);
            } catch (err) {
                console.error("Geocoding process failed:", err);
            }
        }, 300);
        
        input.addEventListener('input', (e) => {
            const val = e.target.value;
            if (val.trim()) {
                clearBtn.classList.remove('hidden');
            } else {
                clearBtn.classList.add('hidden');
            }
            performSearch(val);
        });
        
        document.addEventListener('click', (e) => {
            if (!input.contains(e.target) && !suggestionsDiv.contains(e.target)) {
                suggestionsDiv.classList.add('hidden');
            }
        });
        
        input.addEventListener('focus', () => {
            if (input.value.trim() && suggestionsDiv.children.length > 0) {
                suggestionsDiv.classList.remove('hidden');
            }
        });
    });
}


